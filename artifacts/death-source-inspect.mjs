import fs from 'node:fs/promises';import {GLTFLoader} from 'three/addons/loaders/GLTFLoader.js';import * as T from 'three';globalThis.ProgressEvent??=class{};
const b=await fs.readFile('public/models/modular-hero/modular-hero.glb'),g=await new GLTFLoader().parseAsync(b.buffer.slice(b.byteOffset,b.byteOffset+b.byteLength),'');const clip=g.animations.find(c=>c.name==='Death01'),m=new T.AnimationMixer(g.scene);m.clipAction(clip).setLoop(T.LoopOnce,1).play();console.log(clip.duration);for(const u of[0,.25,.5,.75,.999]){m.setTime(clip.duration*u);g.scene.updateMatrixWorld(true);console.log(u,...['DEF-hips','DEF-head'].map(n=>g.scene.getObjectByName(n).getWorldPosition(new T.Vector3()).toArray()));}
