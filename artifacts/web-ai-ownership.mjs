import fs from 'node:fs';const p='src/engine/web-zip.js';let s=fs.readFileSync(p,'utf8');s=s.replace('const p=new THREE.Vector3();for(let i=1;i<=24;i++){p.copy(f.pos).lerp(new THREE.Vector3(dest.x,dest.y,dest.z),i/24);','const p=new THREE.Vector3(),end=new THREE.Vector3(dest.x,dest.y,dest.z);for(let i=1;i<=24;i++){p.copy(f.pos).lerp(end,i/24);');s=s.replace('if(!f._openSky||f.flying||!f.alive||f.staggerT>0','if(!f._openSky||f.flying||f._grapple||f.hanging||f.launchT>0||!f.alive||f.staggerT>0');fs.writeFileSync(p,s);
