import {chromium} from 'playwright';import {mkdir} from 'node:fs/promises';await mkdir('artifacts/marketing/modular-character',{recursive:true});const b=await chromium.launch();const p=await b.newPage({viewport:{width:1200,height:900}});p.on('pageerror',e=>console.log(e.message));await p.goto('http://127.0.0.1:5184/character-foundation.html');await p.waitForFunction(()=>window.FOUNDATION);for(const m of ['A_TPose','Punch_Cross','Sword_Idle','Sword_Attack']){await p.evaluate(m=>FOUNDATION.set(m,.4),m);await p.screenshot({path:`artifacts/marketing/modular-character/${m}.png`});}console.log(await p.evaluate(()=>({clips:FOUNDATION.clips.map(c=>c.name),bones:FOUNDATION.hand.name})));await b.close();
