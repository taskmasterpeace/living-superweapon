import{readFile}from'node:fs/promises';import{GLTFLoader}from'three/addons/loaders/GLTFLoader.js';const b=await readFile('public/models/modular-hero/modular-hero.glb'),g=await new GLTFLoader().parseAsync(b.buffer.slice(b.byteOffset,b.byteOffset+b.byteLength),'');g.scene.traverse(o=>{if(o.userData.slot==='arms')console.log(o.name,o.morphTargetDictionary)});console.log(g.animations[0].tracks.filter(t=>t.name.includes('morph')).map(t=>t.name));
