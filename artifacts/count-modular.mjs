import{readFile}from'node:fs/promises';import{GLTFLoader}from'three/addons/loaders/GLTFLoader.js';import{MODULAR_RECIPES,applyModularRecipe}from'../src/engine/modular-costume.js';const b=await readFile('public/models/modular-hero/modular-hero.glb'),g=await new GLTFLoader().parseAsync(b.buffer.slice(b.byteOffset,b.byteOffset+b.byteLength),'');const meshes=[];g.scene.traverse(o=>{if(o.isMesh)meshes.push(o)});for(const [id,r]of Object.entries(MODULAR_RECIPES)){applyModularRecipe(meshes,r);console.log(id,meshes.filter(m=>m.visible).reduce((n,m)=>n+(m.geometry.index?.count??m.geometry.attributes.position.count)/3,0));}
