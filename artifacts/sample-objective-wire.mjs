import{readFile,writeFile}from'node:fs/promises';const path='src/engine/convoy-operation.js';let s=await readFile(path,'utf8');s="import {sampleObjective} from './sample-objective.js';\n"+s;s=s.replace('if(this.hud)this.hud.textContent=`${this.assignment.toUpperCase()}', 'if(this.hud)this.hud.textContent=sampleObjective(this.g,operationGuidance(this))||`${this.assignment.toUpperCase()}');await writeFile(path,s);
