import fs from 'node:fs';const p='docs/gameplay/ANIMATION_CONTENT_WORKER_HANDOFF.md';let s=fs.readFileSync(p,'utf8');s=s.replace('Current library coverage is the four imported banks; procedural flight, grabbing and other runtime poses still require separate catalog adapters.','Current library coverage is the four imported banks plus two native paired-hold previews (hostile grab and friendly carry). The holds use the current Fighter animator, allow holder/receiver selection, and cannot be assigned as imported clips. Their two-second preview interval is not combat duration. Procedural flight and other runtime poses still require separate catalog adapters.');fs.writeFileSync(p,s);
