import {chromium} from 'playwright';const b=await chromium.launch(),p=await b.newPage();await p.goto('http://127.0.0.1:5184/character-foundation.html');await p.waitForFunction(()=>window.FOUNDATION);console.log(await p.evaluate(async()=>{const {setModularMuscle}=await import('/src/engine/modular-face.js');const m=FOUNDATION.meshes.find(m=>m.userData.slot==='arms'),a=Array.from(m.geometry.attributes.position.array);setModularMuscle(FOUNDATION.meshes,1);return {noopMaximumVertexMovement:Math.max(...a.map((v,i)=>Math.abs(v-m.geometry.attributes.position.array[i]))),bind:m.bindMatrix.elements,inverse:m.bindMatrixInverse.elements};}));await b.close();
