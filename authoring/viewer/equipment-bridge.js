// Equipment preview: M3 fills this in (mount a package GLB on the production hand socket,
// solve the support hand, verify muzzle/magazine/holster). M2 ships the seam so the viewer
// reports honestly rather than pretending.
export async function loadEquipmentPreview(pkg,stage){
 return {clips:[],bodies:[],note:'equipment preview arrives in M3'};
}
