# LSW full-brief completion ledger

Newest rifle diagnosis: absent city pedestrians were still consuming bullets and receiving explosion damage in PowerWorld/training. Fixed all three paths with the existing civilian-theatre policy; nine regressions preserve city collateral and reject invisible collisions. Related suites/build pass. Two post-fix native wide/portrait standing/reload/crouch/prone routes now pass with0 civilian hits; wide actual contacts ~211u. Earlier mouse-event/clock-timing failures remain separately documented, not erased. See `.dream-loop/2026-09-10-civilian-theatre-collision.md`. Full objective and realistic/performance gates remain ACTIVE.

Status correction: scoped-rifle functionality is **PARTIAL**, not accepted as repeatable. Earlier wide/portrait three-stance routes passed, but latest release checks reproduce missed prone/crouched shots. Below-ground RECON muzzle is fixed; the remaining misses are not explained. See `.dream-loop/2026-09-10-precision-rifle.md`; trace launch/contact before moving on to arsenal expansion.

Newest September 10 precision-rifle pass: RECON slot 3 has held-RMB third-person magnification, finite long-range rounds, five-round magazine/reload, and saved Studio lifetime/zoom. Fixed 162-unit free-aim ceiling for rifles, ignored authored lifetime, zero-blast fallback, zoom framing, and RECON's below-ground prone muzzle (missing soldier equipment setup). Native wide/portrait standing/crouch/prone hits at ~196–199 units and phased placeholder audio pass; retained earlier unexplained miss and visual limitations in `.dream-loop/2026-09-10-precision-rifle.md`. Related tests/build pass. This is one scoped slot, not nine weapons; full goal/realistic10/10/mixed60FPS remain ACTIVE.

Newest September 10 controls pass: soldier held Shift sprint, E interaction/boarding and 1–6 direct selection of existing attacks are wired without changing hero controls. Studio sprint multiplier saves/reloads into actual gameplay. E exits scouts but stays right rudder inside aircraft; J remains aircraft exit. Fixed boarding-prompt overlap exposed by native capture. Final native 16:9/9:16 Studio-save→sprint→selection→walk→board→drive→exit routes, related regressions and 363-module build pass. See `.dream-loop/2026-09-10-soldier-controls-and-boarding.md` for retained failures and limits. Nine-gun inventory, Tab commands, full air/visual/performance acceptance remain OPEN; full goal ACTIVE.

Newest September10 prone pass: Z prone/stand, real slow crawl, lower camera, supported rifle and sideways low reload; shot/beam and swept wall bounds follow the extended body. Corrected stopped crawl, buried legs/elbow and low-reload floor penetration. Eleven targeted tests plus related regressions, final native16:9/9:16 sequences and Studio Front/Side/Rear rehearsals pass. See `.dream-loop/2026-09-10-prone-ground-contact.md` for exact evidence and unresolved first camera jump/custom geometry/stance-extreme limitations. Full infantry/air/visual/performance goal remains ACTIVE, not10/10.

Newest September 10 rifle action: physical magazine extraction/reseat, charging-handle gesture and support-hand recovery now use the existing ammo/audio clock. Native standing and crouch-moving reloads pass at 16:9 and 9:16. Studio Reload magazine sequence saves/replays duration and reports ammo/phase. See `.dream-loop/2026-09-10-rifle-magazine-sequence.md` for rejected upside-down first pass, corrected contact tests, evidence and limitations. Existing rifle family only, not nine finished weapon animations or fitted external rifle. Prone/remaining infantry/air scope and realistic10/10/mixed60FPS still ACTIVE.

Newest September 10 soldier action: G/selected grenades now have an actual hand-held wind-up, release of the same shell, and recovery while retaining the rifle. Interruptions prevent delayed throws; phase-specific replaceable audio plays once. Studio throw preparation/recovery saves and replays; remote rehearsal timing repaired. Native standing/crouch-walking sequences at 1600×900 and 900×1600, staged saved-profile inspection, 14 targeted tests plus related suites/build pass. See `.dream-loop/2026-09-10-soldier-throw-sequencing.md` for failures, scope and evidence. Physical grenade inventory, magazine manipulation, prone and remaining infantry/air systems are NOT complete; full goal/10⁄10/mixed60FPS remain ACTIVE.

Newest September10 combat visibility: chase hit directions now report camera-relative player bearings correctly and use thin viewport-edge ribbons instead of90px interior arcs;12cardinal responsive cases/cleanup pass. Body-contact beams gain axial core read-through without reducing side/free-travel radiance or changing simulation.239related tests/build pass; native SOL146contacts and short charged KANO105contacts captured. See `.dream-loop/2026-09-10-combat-visibility.md` for retained native failures: near-body pending shot, held beam ending after contact/KO, and non-engaging hostile runs still need lifecycle diagnosis. Do not declare all beams stable or repeat opacity tweaks. Full infantry/air/gadget/editor/audio scope and10/10/mixed60FPS remain ACTIVE.

Newest September10: upward two-hand flight startup no longer stalls behind the torso; collision-aware shoulder routing and true XYZ shoulder midpoint preserve captured aim, anatomical readiness and prone travel.459 related tests plus9 terrain tests/build pass. Native sun-facing washout corrected by background-only HDR highlight compression (37%→3.1% near-white canvas). Actual moving upward charged emission/release/recovery recorded. Native hostile SARGE guard616contacts and player SOL guard275contacts now verified; no guard balance code changed. See `.dream-loop/2026-09-10-upward-casting-and-sunlight.md` for failed runs and limits. Next beam receiver/glow and oversized incoming-hit UI readability, then full infantry/air scope. Full goal ACTIVE;10/10/mixed60FPS not met.

Newest September10 beam pass: Studio selectable defender **Advance · real pressure** integrates actual movement, damage, guard and beam pressure with readouts. Production absorbed endpoint tangents no longer reverse shove/reaction; same-receiver packet ownership preserves incoming flow, verified30/60/120Hz.160 focused tests/build pass; native Studio comparisons and135 native gameplay contacts recorded. Wider184/185 tests expose a still-open steep upward10/75degree two-hand startup stall (reproduced alone). See `.dream-loop/2026-09-10-beam-pressure-and-flow.md`; diagnose that next, then hostile beam acceptance and full infantry/air scope. Full goal ACTIVE;10/10/mixed60FPS not met.

Newest September10 infantry pass: PowerWorld soldier **hold C crouch** now lowers actual body, camera, aim/contact center and hurt height without triggering guard/evade. Studio crouch/crouch-walk and paid crouched firing rehearsals added. Empty-magazine reload exposed a1.793u rifle-hand snap; continuous shoulder/barrel/rigid support transition fixes it. Final307 focused tests/build pass, native crouch/moving fire/release/R reload16→30 recorded, plus close Studio inspection. Prone, dedicated magazine-handling animation, remaining control mapping, visual and full combat/performance acceptance remain open. See `.dream-loop/2026-09-10-soldier-crouch-and-rifle-reload.md`. Full goal ACTIVE.

Newest September10 rifle pass: final wrist aiming now resolves held weapon volume against the actor's torso as well as cover. The preserved phase9/frame120 regression is fixed; final **189/189** focused tests and358-module build pass. Native SARGE walk/fire -> carried Jump Jets -> airborne moving fire/turn -> release -> flight-permission expiry recorded without actor edits, alongside close Studio phase9 sequence. Rifle gesture/vest realism and full beam/infantry/air/performance acceptance remain open. See `.dream-loop/2026-09-10-rifle-body-clearance.md`. Full goal remains active.

Newest September10 pass: stronger bounded beam contact illumination using the existing lamp; Studio `impactGlow` saves/reloads into actual gameplay. Native16:9 and9:16 contact/recovery recordings captured. The three airborne non-steering assertions were obsolete upright-chest tests; diagnostic and corrected pelvis/palm/heading invariants pass without production pose changes. Broader testing exposed a **different deterministic rifle-to-torso crossing at phase9/frame120**, now preserved in the phase-sweep test. See `.dream-loop/2026-09-10-impact-light-and-flight-contract.md`. Full goal remains active.

Latest September10 grounded beam pass: PowerWorld grounded targets now receive accepted-hit upper-body reactions without flight permissions; their neutral arm clearance also survives brace recovery. **126/126** focused tests and358-module build pass. Native frontal held-ray→surviving recovery recorded with real hand-height proof. Broader flight-support suite has **three unresolved failures**, and earlier native hand-height intermittency remains recorded. Full goal/visual/performance acceptance remains open. See `.dream-loop/2026-09-10-grounded-beam-reactions.md`; continue with flight-support diagnosis and impact presentation, not another beam-speed/library pass.

## Active continuation — late September 9, 2026 (supersedes historical pause below)

The user subsequently approved all three soldier/combined-arms/aerial-punch concepts and requested five-minute Dream Loop continuation until the completion threshold. Heartbeat `finish-lsw-superhero-combat-and-systems` is ACTIVE. The earlier 21:30 deadline and paused-design note below are historical, not current stop instructions. Work remains on the actual third-person `powerworld.html` game.

Latest aircraft aiming pass: helicopter/jet marker now projects the actual cannon initial trajectory (craft orientation plus inherited velocity), stops at sampled collision, and restores personal crosshair on exit. **84/84** targeted tests and **345-module** build pass. Native helicopter hit, airborne marker checks, and separate jet walk/board/takeoff/fire/short-land/exit pass. Full route failed at scout boarding; very short cannon taps were missed; crosswind prediction is not implemented. Failures are preserved, not replaced by the narrower successes. See `.dream-loop/2026-09-10-aircraft-aim-pass.md`. Visual target still unmet.

Latest correction pass: VEGA Rush Combo uses actual fist contact and prevents the attacker continuing through the victim during hitstop/recovery. WEBLINE has a visible traveling LMB snare/pull and Shift surface zip/wall hold (Space release); not pendulum swinging. Soldier anatomy and actual two-hand rifle contact improved. 202 relevant tests and the 344-module production build pass. Both web actions have normal-spawn native-input browser evidence. Remaining shoulder/body-to-vest clipping is explicitly recorded. See `.dream-loop/2026-09-10-contact-web-pass.md` and `docs/VEGA_RUSH_CONTACT_REVIEW.md` for evidence setup and residuals.

Follow-up: shared flurries now validate 3D range, line of sight, body corridor and destination before relocating; interrupted/retired combos cannot resume unexpectedly. Airborne flurry effects stay at the victim. Final **216/216** relevant tests and **345-module** build pass. Normal-spawn native RMB Spider Flurry delivered six hits/60 damage with zero faults. See `.dream-loop/2026-09-10-flurry-safety-pass.md`. This is collision/lifecycle repair, not a claim of authored flurry animation or global swept airborne body collision.

Preceding structural pass: connected graded military apron/road/runway, authored GLB outpost buildings, accessible scout/helicopter/jet parking, and an explicit Free practice menu option. Native normal-spawn walking, scout driving, helicopter landing and short jet landing passed in Free practice; Clone Recovery combat approaches failed and remain open. See `docs/FRONTLINE_OUTPOST_PASS.md` and `.dream-loop/2026-09-10-outpost-pass.md`. The approved visual threshold has NOT been reached or independently scored; scene/character realism, further clipping and aircraft combat/boundary work remain.

Updated September 9, 2026. This is the continuation loop's authoritative progress map, not a claim that the game is complete.

## Latest creator correction — correct game first

**Latest design discussion — approved fantasy, first slice proposed:** Creator selected **Hero on the Front Line**, with clone infantry and superheroes as fast Living Superweapons. Beams must travel faster while retaining the traveling-stream identity. Blood can be captured, returned and studied to grant selected superhero attributes, create further Living Superweapons or temporarily empower clones; countermeasure research is welcomed as a later possibility, not an existing feature claim. Creator liked Vega's airborne melee impact/knockback but could not see an appropriate flying-punch animation; preserve that impact while reproducing the presentation gap. Data identifies Vega's `Rush Combo` (`type: melee`), whose existing handler sets `punchPose`; no root cause is proven yet. They also want camera crews visibly used and an event-based audio/dialogue harness with character-specific cooldowns and spam prevention. Favor small combat encounters within larger maps before large-war simulation. Proposed order: airborne melee and faster beam feel, camera-crew/audio integration in the same encounter, one clone squad/weapon-role slice, then blood extraction/research. This is a proposed scoped plan, not a resumed build loop or completion claim.

**September 9 — new direction, implementation paused:** The creator played `powerworld.html` and confirmed this is the intended game. They then explicitly requested stopping here, installing Dream Loop, studying Bohemia Interactive CWR for a Three.js superhero/soldier/vehicle world, and asking questions before starting the new loop. The existing heartbeat `finish-lsw-superhero-combat-and-systems` is now **PAUSED** by user instruction. Do not resume old implementation automatically. Dream Loop was installed at `C:/Users/taskm/.codex/skills/dream-loop`; no target image or new build loop has started. Next: clarify the core playable roles and world scope, agree on a design and visual target, then run approved iterations with native gameplay/performance gates alongside visual critique. CWR is a source-level architecture reference, not an authorized asset pack: its source is GPL-3.0-or-later with additional terms, and its separately licensed Arma-only assets must not be imported into this game without separate permission.

The user explicitly rejected the city footage: their intended game is the different-map third-person version without the city's height-band behavior. The matching existing entry is **`http://127.0.0.1:5180/powerworld.html`**, not `/`. `src/pw-main.js` boots `PROFILE_POWERWORLD`, the native rock/spire arena and open-sky controls; Studio's actual Play Test already navigates there.

**Stop the mistaken city integration pass.** City HUD correction worker was interrupted; preserve its edits for review, no bulk revert. The city-articulated-combat preflight is not the next implementation task. The intended arena already enables those native `_openSky` paths; porting them into the city does not answer the user's request. Earlier city-camera implementation/evidence below is historical, not acceptance of the intended game. Keep future game/editor work and verification on the actual PowerWorld page unless the user explicitly asks for the city.

Fresh actual-page check: `tools/powerworld-entry-browser.mjs`, `artifacts/powerworld-correct-entry/`, exit0/zero errors. Real title Start, native middle-click capture/mouse yaw+pitch, Space ascent+hover, D strafe retaining aimed heading (aim dot.99994), W three-dimensional forward input, C guard and Escape pause/release passed, with AI/game update unchanged. Mode/stage/openSky are genuinely PowerWorld; police false; Perspective73.74. Parent inspected the native rock-arena entry screenshot and queued this exact browser URL in Codex. This verifies controls/identity, not whole-game polish.

Fresh extended run `artifacts/powerworld-correct-controls/` also verifies real grounded W at y0/grounded gait follows camera heading (dot.99969), then native aerial controls/guard/pause, zero errors. Worker confirmed **no post-review HUD production fix was applied**; only test harnesses/report changed after freeze. Vite session14885 remains on5180; all test browsers closed. The screenshot/link is provided to confirm the intended map before further substantive changes; do not remap `/` or resume city porting by assumption.

## Source requirements and authority

- Current pasted brief: `C:/Users/taskm/.codex/attachments/2bcce36c-a5d7-40eb-aeb3-62c0afa8c1e2/pasted-text.txt`.
- Linked detailed brief (corrected missing slash in pasted path): `C:/Users/taskm/.codex/attachments/24eebba6-00c6-44eb-98ed-27f16ec4a37d/pasted-text-1.txt`.
- User explicitly requested `/loop until you finish all that I asked for`. Proceed autonomously in tested passes; do not stop after a partial effects pass and label the whole brief complete.
- Thread heartbeat: `finish-lsw-superhero-combat-and-systems`, every ten minutes, **PAUSED by the latest user stop instruction**. Do not create duplicates or resume until the new direction is approved. The older movement goal is paused; the agent cannot resume it via goal tools.

## Requirements

| Area | Status | Remaining acceptance |
| --- | --- | --- |
| BFP reference/camera baseline | Existing, needs final feel review | Preserve current BFP framing; use verified code/footage evidence; inspect live input rather than only posed shots |
| Ground locomotion, clipping, independent lower/upper body | Scoped bridge/air-cast/source-jump repairs implemented and reviewed | Full movement/aim matrix and subjective input feel still need acceptance; an existing stage rock obscures the final jump landing shot; anatomical aim limits and wall/muzzle occlusion remain part of broader review |
| Hand/twin/alternating/fist/chest/eye/weapon origins | Existing partial; audit active | Coverage across grounded, jumping, hover, flight, ascent/descent; no generic pose fallback silently counted as authored support |
| Contact/shield/charge effects | September 8 scoped pass verified | Preserve verified 197-test baseline; extend charge/recoil/ground-air coverage where audit finds real gaps |
| Construct particle formation / resources | Wall/tank resource slice implemented and reviewed; base fist/hammer/turret formation existing | Resource policies for other forms remain a follow-on; no complete arbitrary-shape claim |
| Modular forearm nanites | Tasks1–3 fitted assembly, cannon and physical shield contact verified; Task4 Studio active | Public six-body/corner contact+moving repair passed; two16-module stress repeats passed, but one unexplained browser crash remains open. Portable Studio and full lifecycle/feel acceptance remain |
| Studio presentation authoring | Effects profile verified; extension open | Formation shape, attachment, material, charge stages, impact response, moving previews, save/export/import |
| Sound/dialogue harness | Opt-in native Studio audio implemented and reviewed | Motion-only sound and new recorded dialogue remain open; cue inventory/export ready, AAI service clarification pending |
| Two-slot wheel and compact attack icons | Native browser audit passed | Normal wheel primary, RMB+wheel secondary suppression, pause/cancel, native aimed strafe and compact HUD verified; ongoing real-player feel review |
| War correspondent/highlights | Recording/replay verified; reporter obstruction repaired | 51 focused tests and final native clear-lane imagery; broad city/dynamic occlusion and cinematic taste still need review |
| Military/escalation/vehicles | Existing modules need audit | Actions increase threat, graduated response, distinct tank/plane/helicopter simulation and eventual player control; no map redesign |
| Inhibitors, aliens, human zombies/hordes | Native inventory and staged brief prepared; not implemented | Selective suppression, explicit allegiance, same-person human infection, finite spit and measured hordes; ambient civilian handoff remains a required separate gate; flight stays a valid escape |
| Scalability/performance | Bounded VFX checks verified; full target open | Document hardware/browser, simultaneous effects/actors, pauses/slowmo, KO/rematch cleanup, real frame measurements |

## Current plan

`docs/superpowers/plans/2026-09-08-completion-continuation.md` is historical pending the latest Dream Loop design discussion above. The city camera/HUD plan is stopped, not completed. PowerWorld remains the accepted foundation; do not execute further gameplay work before the requested questions/design approval.

## Decisions

- Preserve the existing dirty checkout on `codex/construct-effects-pass`; no bulk stage/reset/commit and no copying the user's uncommitted game into a fresh worktree.
- Work from existing runtime seams rather than rebuilding systems already present. Audit movement before changing animation math.
- The first resource-construct pass will preserve legacy duration behavior unless the new lifecycle is explicitly configured. Recurring drain and damage-backed energy are distinct modes; no irreversible global rebalance.
- Existing audio assets/DSP can be used now. The new request supersedes the older prohibition on future AI-generated audio, but no provider access is available or authorized for purchases. Exact AAI provider requested asynchronously; prepare local cue and dialogue manifests meanwhile.
- War World animation skill references a different `src/client` rig layout absent from this repository. Apply its motion-through-time/volume/contact evidence rules to the actual `src/engine` rig; do not pretend absent TypeScript/Vitest commands or clips were verified.

## Evidence already available

`docs/COMBAT_EFFECTS.md`, `artifacts/combat-effects/`, `artifacts/beam-pressure/effects-final/`. Last focused suite: 197 passed; production build passed with existing large-chunk advisory. These do not prove the entire brief or subjective AAA feel.

## This continuation

- Studio icon correction now verified at actual5180: unfiltered native trace and8s native moving/mixer recording both pass with zero errors/failed samples and no interceptions. `docs/reports/2026-09-09-nanite-studio-moving-audio.md` records the scope: two launches, actual local cell absorb/residual/repair, but distant orbit framing is not fine-detail visual acceptance and284.9ms loop reset is not hitch-free. Separate prior crashes remain open.
- Parent root lifecycle harness first failed because its held attack activated native Second Wind instead of reaching KO. This is a fixture error, not evidence of broken pointer release; corrected fixture now uses the real downed-slam finisher and asserts actual KO before checking release. Fresh rerun pending after worker GPU lane. Camera worker is authorized only clock/foe and phone mood layout correction, with actual combined-HUD RED/GREEN; original exact combat→2P replay is upright, so no speculative camera rewrite.
- Independent camera review confirms the new view/input separation is sound but the clock/foe collision needs correction; exact root2P/body evidence is still running. It also confirms a separate delivery boundary: city BeamHose launch/pose/readability remain `_openSky`-gated. A rear camera alone does not deliver the open-sky final-emitter/animation path. Preserve player/bot symmetry; do not wire authoritative readiness to a camera flag. Native city emitter parity needs a dedicated follow-on before claiming the game's combat matches the preview.
- Parent prepared (not implemented) `docs/superpowers/plans/2026-09-09-beam-appearance-authoring.md`: existing build/temper/primary-color Studio controls, neutral-white shader correctness and same-physics reference comparison. Camera correction and city delivery gaps retain priority. Parent recorder now removes favicon interception in all modes; future acceptance must run unfiltered, not hide the diagnosed missing icon.
- Actual user-port delivery gate now also passes at `http://127.0.0.1:5180/` (fresh separate browser, real root menu,0errors; no existing-tab/storage reset). Parent freshly reran all132 planned native tests and build281/5.32s. Independent camera spec/quality review is active against `artifacts/main-combat-view-review.patch` (honestly whole HEAD-to-current for dirty tracked files), with frozen review snapshots for later fix diffs. Task is NOT accepted yet: parent image review found visibly inverted/clipped2P handoff, phone mood over meters, and city clock over hostile HP/name. Worker keeps runtime frozen while reproducing these and completing the original small/large cannon/guard near-cover gate; combine verified findings into one correction round.
- Studio404 cause is now identified by an actual unfiltered short native-RAF trace: both console/CDP failures name `/favicon.ico` (boot and playback), while all sampled audio buffers load. `studio.html` lacks the self-contained icon already present in `index.html`. `artifacts/nanite-resource-trace/` preserves the intended failing zero-error gate and exact URLs. Reusing the existing icon is queued after active camera work; not yet fixed. The separate Chromium crash remains unexplained.
- Main root layout correction visually rechecked at1600×900 and1100×720: cityplate moved off the body to upper-left, selected pair retained, duplicate large ability cards reduced to icon/hotkey rail; news remains active. Parent fresh actual-root native pointer/Space/W/mouse/Escape capture passes with0errors at `artifacts/main-entry-camera-final-action/`; silentH264 clip available. A prior60RAF hold failed by releasing inside native ballistic jump at high refresh; the harness now waits for actual flight under a real held key, not a simulation override. Full worker contact/2P/mobile gate and independent review remain open.
- Nanite recording-resource diagnostic fully read: all266 manifest MP3s exist, but prior404 URLs were not saved; no favicon/crash attribution is established. Parent recorder now has phase-tagged console/HTTP/CDP provenance with incremental artifact persistence and a short native-RAF trace-only mode (syntax checked, not yet run). No runtime/audio asset changes; camera GPU tests retain priority.
- Parent actual root/UI AFTER now passes the BFP gate at1600×900/RTX4090 with0errors: perspective73.74°, centered rear view, crosshair shown, Free Roam/White City/police/news intact and `_openSky=false`; no PowerWorld identity spoof. Screenshot inspected. This is not final acceptance: central cityplate still overlays the legs and lower docks are heavy; returned to the sole worker before full input/motion/responsive verification. Evidence/report: `artifacts/main-entry-camera-after/`, `docs/reports/2026-09-09-main-entry-camera-acceptance.md`.
- Nanite Task4 two-P2 correction is independently approved for spec/quality; parent fully read final worker/reviewer appends and inspected KO/unavailable-source images. Native zero-time KO pose ownership and dead-source new-emission fixes are accepted; incoming high-blast image is telemetry context, not motion evidence. Overall nanite moving/audio/reliability gates remain open.
- Main-city integrated camera/input/HUD Task1 is now assigned to the sole implementation worker after the nanite runtime correction froze. Root-entry expected-BFP RED already exists; parent harness now also records untouched native camera vectors, projected part centers and city-overlay rectangles, and requires the separate `combat-chase` class without `powerworld` identity. No root fix is claimed yet. Pad/touch scope addition: active-view L1+R3 explicit lock (R3 consumed through release; ordinary item and descend preserved), plus a dedicated accessible touch lock button; no changes to legacy/P2 mappings.
- Nanite review correction runtime is frozen: worker98/98 focused checks/build280 pass; independent original-repro probes now preserve KO matrices/clocks and prevent new dead-source emission while retaining committed projectiles. Final visible evidence/report and reviewer verdict remain pending; broader moving/audio capture failures remain open.
- Ruling: main root one-player city gameplay should use BFP camera/input/HUD by default, with city simulation identity intact — this addresses the user's actual root-page failure — a later explicit isometric preference can be added if desired, but changing `_openSky` or redirecting does not qualify. Plan `docs/superpowers/plans/2026-09-09-main-game-combat-view.md` is authored/self-reviewed from the fully read preflight. Actual root/UI baseline passed with iso/no crosshair and active police/news; the expected-BFP version then failed exactly `iso !== chase`, before implementation (`artifacts/main-entry-camera-red`). Implementation is next after active load-bearing nanite review corrections; no camera fix claim yet.
- Audio description v2 correction accepted after independent source review:21entries (new direction-only beam-cover-contact), corrected real heavy/gun/blast/landing source lists and accepted-Fighter contact scope;41 focused tests/build280 and fresh actual dialog/export/no-autoplay/mobile browser pass. Main fully read corrected report/source. Actual cover/construct contact audio remains an open native wiring requirement, not completed by describing it. User's two original beam references are preserved unchanged in `docs/reference/beam-narrow-user-2026-09-09.png` and `beam-pressure-user-2026-09-09.png`.
- Nanite Task4 independent review has two confirmed load-bearing Studio issues, under one authorized correction round: zero-time/settle calls `_animate` on a KO ragdoll and moves rendered limbs; an incoming actor killed by a valid tuned cannon can still emit a new2.4s sample. Worker must use native ragdoll apply/sync without clock advance and suppress only new dead-actor samples, preserving surviving ordnance. Do not mark Task4 accepted until independent repro checks pass. Scripted incoming actor repositioning remains a labeled precision fixture, not real source-weapon/AI firing proof.

- Audio description v2 slice now integrated:20 entries with attack type/phase/playback/timing and native-vs-direction-only status, both reference-family briefs in the existing collapsed section and v2 export;14 original dialogue candidates retained. Four data REDs and an actual dialog RED preceded the fix;35 focused tests/build280 pass. Real desktop/390px dialog/export/no-autoplay browser checks and inspected images pass with zero app errors (known missing favicon answered204 only in fixture). No new sounds/providers or changed native playback. Guide `docs/STUDIO_AUDIO.md`; artifacts `audio-cue-brief`. This supersedes the earlier incomplete-UI note below; independent scoped review is still pending.
- Final Task4 nanite worker report fully read: `docs/reports/2026-09-08-nanite-forearms-report.md`,785 tests/build/UI zero-error evidence. Independent review is now active, runtime frozen. Parent third fresh moving/audio recorder process crashed before completing warmup (`artifacts/nanite-studio-reel-final`, faviconRequests empty); previous two captured loops failed on console404s. This is a new reproducible outcome to investigate, not proof of the same cause as the earlier eight-Fighter crash. No clean moving/audio acceptance or general reliability closure.
- Beam reference preflight fully read: existing ray/hose/torrent and temper data, missing Studio appearance axes, and actual neutral-white shader uniform turning red confirmed. Proposed later appearance-only authoring/shader correction must keep radius/DPS/travel/contacts unchanged. Main-game city camera/input/HUD integration preflight is now priority after the user's actual root-page screenshot; no redirect or world-mode identity spoofing.

- September9 new user references distinguish a thin restrained white beam from a broad blue-white pressure beam. Read-only preflight delivered `docs/reports/2026-09-09-beam-reference-preflight.md`; main has not yet fully read the final report. Existing data offers ray/hose/torrent, but Studio appearance authoring and achromatic shader handling need investigation. No visual matching or beam physics change is claimed. Parent added v2 audio brief metadata with20 explicit attack-type/phase/playback/timing/status entries and two clearly direction-only reference-family sound briefs; four new REDs now pass alongside seven audio tests. Existing Studio dialog/export filename is not yet updated to display these new fields: this slice is incomplete. Source files changed only in `src/data/audio-cues.js` and its new focused test; no new sound assets/provider integration.
- Latest user screenshot at the root5180 URL exposes a delivery integration gap: `index.html` loads `src/main.js` → `PROFILE_FULL` (city/training); only `MODE_IMPL.powerworld.setup` enables `ms.chaseCam`. `cameraDrive` otherwise uses `followHumans` → isometric `World.follow`; compact HUD rules are also scoped to `body.powerworld`. Studio Play Test navigates `powerworld.html`, so scoped Studio/PowerWorld verification did not verify the normal root city experience. This is code-confirmed mode gating, not evidence of bad user camera settings or stale cache. Current in-app browser inventory surfaced no attachable tabs, so the user's live storage was not inspected. Latest turn diagnoses this mismatch; no root-route/city-camera change has been implemented. Prioritize an explicit main-entry/city gameplay integration gate, preserving the city systems, not merely redirecting to a test page or globally retuning the BFP lens.
- Parent native nanite Studio action/audio capture has actual8s loops, contacts and two paid launches, but both full-browser attempts currently fail on two browser-console404s without page-response URLs; cause still under diagnosis, not a clean browser pass. Artifacts `nanite-studio-reel` and `nanite-studio-reel-retry` preserve the failures. Task4 worker continues final native metadata validation/report; no final Task4 acceptance yet.

- Requirements and referenced brief read; automatic continuation installed.
- Inventories delivered: `docs/reports/2026-09-08-movement-gap-audit.md` and `2026-09-08-systems-gap-audit.md`. Source inspection distinguishes real military response/infantry, wheel controls and correspondent from missing vehicles/nanites/infection/resource lifetimes.
- Audible Studio combat harness delivered with opt-in Sound transport, actual local recordings, silent scrub/pause, native energy warnings, 14 cue families and 14 unrecorded dialogue candidates/export. Scoped independent review approved; 18 focused tests and real AudioContext/analyser browser evidence. See `docs/STUDIO_AUDIO.md`. Future AAI access and motion-only sound remain open.
- Audible proof: `artifacts/studio-audio/kano-native-audio.mp4` includes actual captured Studio AudioContext sound, verified H264/AAC tracks, native beam target damage and zero errors. Scripted Studio rehearsal, not player-input footage.
- Existing two-trigger controls and MERC/BREACH/RECON held-weapon firing passed native browser audits (`artifacts/dual-trigger/`, `artifacts/military/`). These do not establish complete military AI/vehicles or frame-rate-independent balance.
- Correspondent recording produced valid real scene clips/replays, but image review caught reporter self-occlusion. Physical stand-up staging and cold-focus initialization repaired it; 51 focused tests, independent review, final native opening/impact imagery. See `docs/reports/2026-09-08-correspondent-clear-lane-report.md` for exact limits.
- Ground-air handoff fixes the measured source-stride knee snap and handles hitstop/form cleanup; 18 focused tests, native short-jump physics proof, independent review approved. See `docs/reports/2026-09-08-ground-air-handoff-report.md`.
- Air-cast legs preserve authored travel joints with bounded hover bracing/recoil. 224-test worker verification + native scripted multi-emitter captures; independent scoped review approved. Parent reviewed representative hover/cruise/descent images. This is not a final complete motion-matrix or player-feel approval.
- Live jump imagery exposed unpowered ballistic jump becoming a prone flight pose. Source-authored Jump_Start/Jump_Loop/Jump_Land now wired, with native Studio Ground jump and unchanged physics. Independent review caught additional body/leg snaps at jump→flight and first touchdown; both fixed by finite family-aware bridge. Final original-probe maxima: torso 0.803°, legs 3.08° per first transition frame across 30/60/120 Hz. 93 post-review focused tests and fresh 272-module build passed. Parent final native 120-frame gameplay capture includes all three takes, apex 3.54555 for SARGE, grounded end, upright body and zero errors; keyframes visually inspected. Report: `docs/superpowers/plans/2026-09-08-ballistic-jump-report.md`.
- Resource construct Task 1 native ownership/lifetime/dismissal integration implemented and independently approved. Final 126-test gate and 273-module build pass; reviewer independently passed 15 focused lifecycle cases plus native timed-tank KO/respawn/dismissal. Browser and inspected images show unchanged entry payment, active wall at 10.5 seconds beyond its old 9-second duration, exact depletion at 12 seconds, actual cover removal, low-ki dismissal without payment/denial, and zero errors. See `docs/reports/2026-09-08-resource-constructs-task1-report.md`. Damage mode is only an idle lifetime policy until Task 3; no hit-routing completion claim.
- Resource Task 2 (native moving construct tank) implemented and independently approved after review corrections. Native tracked hull, separately aimed turret/cannon, safe placement/translation/articulation and bounded particle skins; 115 focused tests and fresh 274-module build pass. Final parent browser/image recheck: 961 frames with actual barrel clear of cover throughout, three native shots, 73.1059803 target HP damage, safe thin-wall stop and depletion cleanup, zero errors. Review-found close-target shots, turret acquisition snap and barrel-wall clipping were fixed; independent original acquisition probe now 0.05rad at 60 Hz. See `docs/reports/2026-09-08-resource-constructs-task2-report.md`. No general recovery from external geometry inserted through an occupied barrel is claimed.
- Resource Task 3 native damage receivers implemented and independently approved: 330 focused tests (89 new hit checks) and fresh build pass; reviewer freshly passed 69 selected cases plus concurrent-beam exhaustion at 30/60/120 Hz. Parent browser/images verify direct 20-ki debit, one 15.2-ki native falloff splash debit, actual 48-frame beam travel before any debit, exactly 10 ki over a subsequent quarter-second of contact, no released-tail drain and saturating depletion/cover cleanup; zero errors. See `docs/reports/2026-09-08-resource-constructs-task3-report.md`.
- Resource Task 4 portable authoring/Studio implemented and independently approved: 310 focused tests and fresh 274-module build pass; actual UI editing, inactive saved rates, genuine ORIGIN Will Tank catalog, save/export/import/reload, native gameplay definition parity, 390px layout, silent seek, incoming hits, cannon firing and dismissal all passed with zero app errors. Review-found raw-source validation and scrubbable eight-second endpoint issues fixed and independently retested through accepted controls. Parent inspected final images; actual Studio video/audio at `artifacts/resource-construct-studio/native-studio-tank.mp4`. See `docs/reports/2026-09-08-resource-constructs-task4-report.md` and `docs/RESOURCE_CONSTRUCTS.md`. This closes only the wall/tank first slice; military vehicles, other resource forms and infection remain open.
- Task 4 authoring preflight read: `docs/reports/2026-09-08-construct-authoring-preflight.md`. A demonstrated Studio parity gap authorizes extracting only ordinary Fighter energy regeneration, preserving native arithmetic and update location. Resource-only rehearsal uses positive simulation time once per owner; the 90-frame seek pose-settle pass must not regenerate or spend energy. Preserve timed-only behavior, genuine recipe reconciliation and the separate eight-second inspection clock.
- Task 3 hit-routing preflight read: `docs/reports/2026-09-08-construct-hit-preflight.md`. Preserve native ballistic-vs-explosive behavior, guided early contacts, delayed/remote timing, finite-bottom ricochet normals, final reached beam cutoff, one melee winner and synchronous sibling retirement. No new hit-route implementation or test success is claimed by that report.
- Forearm nanites Task 1 now active under `docs/superpowers/plans/2026-09-08-nanite-forearms-brief.md`: private source-bound state, fitted solid assembly and lifecycle first. Initial integrity/repair/source prices accepted as tunable, unbalanced engineering defaults, not user-supplied numbers. Actual forearm attachment/aim, local physical cells, fit/cleanup and staged public source publication are specified; global guard input policy stays unchanged. No usable cannon/shield or completed nanite implementation is claimed yet.
- Military vehicles brief prepared and fully parent-read: `docs/superpowers/plans/2026-09-08-military-vehicles-brief.md`. Prototype combat/response Tasks 1–4 defaults accepted as unbalanced engineering starting points, not user rulings; no runtime implementation started. Final moving-melee ordering must include current vehicle receivers before contact resolution. Later occupancy/ejection Tasks 5–7 remain design-gated: no arbitrary max-HP trauma or forced KO merely because an exit candidate is blocked.
- Nanite Task 1 completed as a private state/geometry checkpoint: worker 296-test scoped gate/build pass; parent final 286-test hand/form/profile/package gate pass; independent 117-test final review plus 48-case saddle/hand volume and native snapshot ownership probes approved. Parent browser captured nine fitting variants and six moving break/reform variants (81 images), with zero errors and unchanged zero-dt state. Direct reducer damage removes only one shield cell, snapshots return 14→14→15 while walking 8.625 units or flying 50.674 units through repair. Cuff radial clipping, interior assembly paths, open-hand overlap, floating barrel support and key-order-sensitive source retirement were caught and fixed. `artifacts/nanite-assembly/private-moving-assembly.mp4` is a silent, browser-paced 29.546-second inspection recording of four seconds of simulated walking; not a gameplay hit or input-feel demonstration. See `docs/reports/2026-09-08-nanite-forearms-task1-report.md`. Actual cannon use, physical shield contact and public authoring remain Tasks 2–4.
- Nanite Task 2 implemented and under final independent review. Parent fully read its emission preflight and `docs/reports/2026-09-09-nanite-forearms-task2-report.md`. Worker final 312 tests/build pass; the genuine ORIGIN cannon is now published after its native gate, while the shield stays private. Parent `tools/nanite-cannon-browser.mjs` passed three actual Game/controlPlayer DOM-key scenarios: small female mirrored-left ground strafe, male right hover, large procedural right forward flight. Each denied unassembled holding without payment, accepted a fresh paid charge, emitted one current-aperture shot and damaged the target (69.08–69.13 HP); maximum axis error 0.013611 degrees, finite-radius center error zero, zero browser errors. `artifacts/nanite-cannon/results.json` and inspected keyframes record this controlled native-input fixture, not AI/input-feel approval. A longer-charge close-up pass is running because the first hold ended before stage 2 and the small-body inspection framing was too distant. Actual panel contact and portable Studio remain Tasks 3–4.
- Motion-audio preflight fully parent-read: `docs/reports/2026-09-09-motion-audio-preflight.md`. Studio Ground jump already emits native departure/steps; other motion-only previews are silent and legacy steps use a separate sine clock. Accepted direction for a later bounded slice is source/contact-driven cues consuming the existing motion clock once, with no duplicate full-update jump audio. No motion-audio implementation is claimed; local audio unblocks it independently of pending AAI identification.
- Infection/suppression brief fully parent-read: `docs/superpowers/plans/2026-09-09-infection-suppression-brief.md`. Exact native hazards include all-weapon `noPowers`, guard chip not being accepted exposure, truthy zero-damage/splash defaults, deflection extending lifetime/range and ambient pedestrians not being Fighters. Initial active-attack-only inhibitor, independent alien allegiance, explicitly enrolled human-Fighter infection and finite spit/horde prototype settings are accepted as unbalanced engineering starting points for later task-by-task execution, not user rulings or current runtime. Ambient civilian identity/atomic handoff, legal credit, cap-full behavior and measured crowd-scale acceptance stay design-gated and required; no few-Fighter prototype closes the user's horde request. Effects/shields retain priority.
- Task 2 final accepted: review-found grapple→hang delayed firing is fixed and independently reprobed; source-only firm fist replaces the cannon's open-palm carrier without changing the opposite arm. Final 321 tests/build pass, independent nine-case correction gate and shared-manager ownership probe pass. Parent corrected full-stage browser passed all three native motion/body cases, zero errors, full radius 1.8, direct payload 64, native direct+splash HP loss 106.79–106.83, maximum axis error .005501 degrees and finite-sphere center error zero. Final closeups were visually inspected. See `docs/reports/2026-09-09-nanite-forearms-task2-report.md` and `artifacts/nanite-cannon-stages/`; this is a scoped cannon checkpoint, not all-nanite/Studio/player-feel completion.
- Task 3 is now authorized and active under its accepted brief and fully parent-read `docs/reports/2026-09-09-nanite-contact-preflight.md`. Real cell/projectile/hose/fist contact comes first, then shield publication after protection/holes/reform gates. Preserve native deflection unless a currently eligible actual shield panel wins; reflected/rejected cannon hits do not debit a cell. Held beam metadata needs live cell validation before restoring its cap, and full absorption must preserve Guard consequences without body-damage XP or shieldpack consumption. Parent `tools/nanite-contact-browser.mjs` is prepared/syntax-checked only, not run before the protection gate.
- Cannon short native audio/action capture is available at `artifacts/nanite-cannon-reel/native-cannon-audio.mp4` and was shown inline. Test-browser diagnosis found default headless-shell SwiftShader; explicitly using installed full Chromium activates the RTX4090. Corrected warmed recording: 4.421333s H264/AAC for 4.416667s native fixed-input simulation, zero dropped wall time and zero errors. First-use warmup still stalled (1.7188s beyond capped capture intervals); this is recorded as an open prewarm/performance gate, not hidden or counted as solved. No production render/physics change was made by this capture.
- Motion-contact measurements fully read: `docs/reports/2026-09-09-motion-contact-measurements.md`. Actual source/target 30/60/120Hz measurements show 1.50/2.18/3.00 contacts/s for the controlled walk/jog/sprint pure takes; tight height gates miss feet and support AABBs double walking counts. Future cues should consume source stance windows plus final-foot qualification on the existing signed phase. No motion audio, blended-motion or player-feel acceptance is claimed by this read-only report.
- Nanite Task 4 authoring preflight fully read and scoped approach accepted: `docs/reports/2026-09-09-nanite-authoring-preflight.md`. Full native pattern clocks/intent, finite owner HP+KO, separate outgoing dummy totals, exact8s endpoint, transient .25× transport and effective-profile validation before mutation. Task 4 implementation remains gated behind Task 3; no ready editor claim from these plans.
- Cold-frame profiling now identifies substantial synchronous first-use shader waits in the native correspondent/default-canvas path as well as ordinary rendering. See `docs/reports/2026-09-09-native-cold-frame-diagnostic.md`, isolated timing artifacts and actual Chromium CPU profile. A warmed repeat still had a53.4ms first update even though no interval exceeded the recorder's100ms cap; no-dropped-time must not be described as hitch-free. This remains a production prewarm/lifecycle performance gate, not a solved recording-only issue. Parent eight-Fighter/16-module stress harness is prepared/syntax-checked, awaiting public shield protection.
- Motion/audio implementation plan fully read and checkpointed for later execution after Nanite Task4: `docs/superpowers/plans/2026-09-09-motion-audio-bridge.md`. Metadata-only source stance extraction, final-sole qualification, one signed native phase, ordinary physical jump touchdown and positive-time Studio parity are specified. Blend/reverse behavior, exact geometry cap, performance and audible timing remain test/review gates. No runtime or new provider connection is implied by the plan.
- Task3 interim report fully read: `docs/reports/2026-09-09-nanite-forearms-task3-report.md`. Its 631-test result explicitly filters two pending diagnostics and is not a final gate. Independent review corrected their classification to an actual zero-dt raw-aim shield snap, not established rotational collision tunneling; a source-only full-orientation/clearance correction is active. The private browser fixture now uses a real hostile source (native dummies cannot attack): small actual projectile contact preserved HP, reduced one cell 12→8 and Guard 1→.78; a later residual hit broke an actual neighboring cell and reached HP. The complete three-body/hole/reform harness has not yet passed, and a visually excessive residual body flash remains under inspection. Shield is still private.
- Vehicle occupancy preflight fully read: `docs/reports/2026-09-09-vehicle-occupancy-preflight.md`. Controllable vehicles remain required. Existing mount/possession cannot supply honest seat ownership; actual whole-body hatch routes, current clear release volumes, status/KO-before-ragdoll, input, targeting and immutable pilot credit need native tests after real vehicles exist. Parent's recommended design to test is an actual breakaway/open cabin with the remaining wreck still crashing, never invisible hull/teleport/geometry damage. Live-swap refusal while no safe exit exists is a reasonable initial default; cabin exposure, actual geometry and airborne momentum still need execution-plan decisions. Ordinary landed dismount must not silently exclude an eventual deliberate clear-route superhero bailout. No occupancy runtime or final design approval is implied.
- Native prewarm preflight fully read: `docs/reports/2026-09-09-native-prewarm-preflight.md`. Existing synthetic material disposal does not retain unshared programs; Three0.169 `compileAsync` waits only a material's current program and cannot cancel. Later implementation should use one visible same-renderer preparation boundary, actual default/HDR variants and bounded retained render-only families, with guarded readiness or an honest synchronous preparation fallback. No hidden simulation/News recording, quality downgrade or shader-diagnostic removal. No implementation or resolved hitch claim; cold-process and first-real-correspondent measurements remain required.
- Nanite Task3 accepted and Task4 authorized: final unfiltered687 tests/build pass, independent145 contact +50 fit/turn +6 residual/pair selected cases approved. Public native browser passed six body/corner cases:4 absorption without HP loss, actual remaining8 cell break with9.24 residual HP,1.68 HP through the hole,12HP local repair while moving23u; images inspected and0errors. Post-pose precision launch corrected a stale-hole test assumption; independent exact geometry confirmed the earlier neighboring plate interception was valid. Two fresh8-Fighter/16-module pressure/cleanup runs passed with80 owned resources disposed once, but the first Chromium crash remains unexplained and open. See appended `docs/reports/2026-09-09-nanite-forearms-task3-report.md`; not arbitrary-rotation/all-attack/AAA completion. Sticky/armed/boomerang variants, piercing hoses and non-contact/AoE/DoT lanes are not local-cell destruction coverage. Sole worker now implements the accepted portable moving Studio pattern/controls/validation Task4, not military yet.
Newest September10 camera pass: enemy KOs were starting an orbit camera that released pointer lock and cancelled held beams. Actual lifecycle traces isolated it; living third-person players now retain camera/input. RED/GREEN regressions,282 focused tests/build pass; native held KANO99contacts through KO, short-release198contacts, SOL guard139blocked contacts. See `.dream-loop/2026-09-10-live-ko-camera.md`. KO+level-up flash/banner overlap remains visually severe; earlier intermittent startup/AI nonengagement remain unproven, not erased by passes. Next presentation overlap then outstanding infantry/air scope. Full goal ACTIVE;10/10/mixed60FPS not met.
Newest September10 continuation: routine level-up whiteout replaced with small energy pulse; compact separated KO/progression notices replace duplicate comic KO stamps in live BFP view. Native115contact KO/recovery sequence and responsive HUD checks pass. PowerWorld soldiers now G quick grenade/Q carried gadget, preserving selected rifle; native grenade→rifle29→23→jets2→1 verified. Authored-slot/guard tests and broad13-suite regressions/build pass. See `.dream-loop/2026-09-10-ko-readability-and-soldier-shortcuts.md`. Next actual grenade/reload action sequencing, prone/remaining soldier controls, then full air/squad/arsenal/gadget scope. Grenade throw animation and military sound are NOT done. Full goal ACTIVE; realism10/10/mixed60FPS remain unmet.
