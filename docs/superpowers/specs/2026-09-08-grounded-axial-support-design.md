# Grounded axial support

The full independent-combat objective and ten-part attachment remain active.
This pass repairs a measured grounded chest acquisition defect, not broad art,
anatomical, editor or AAA acceptance. Standing +1000u and jogging +250/+1000u
targets can remain pending ten seconds: the source forward lean plus .5
directional pitch and .8 extra chest correction cannot reach the captured ray.

Add one reversible **grounded axial body-support** stage after the existing body
carrier settles and before chest/spine/head/hand articulation. It serves actual
chest powers, including preparation. Predict the requested ray through the native
beam planning interface when present; charges use their current command.
Distribute elevation beyond a .9-radian pelvis-relative pitch target into a
bounded .65-radian whole-body/pelvis lean. These are gameplay pose budgets, not
medical claims. The existing final spine yaw/rate envelope remains authoritative.

Both boot transforms are captured from the already-authored stride. Rotate the
body around the hip, then solve its two-bone legs back to those same world-space
foot targets, retaining fixed segment lengths and source knee bend side. Restore
the boots' world orientation so their soles do not roll through the floor. If a
hip target would overextend a leg, lower the visual body only enough to preserve
reach, rather than stretching a bone or moving the feet. Neither simulation
position/velocity, aim, camera, gravity nor ability payment changes.

Review refinement: bone lengths must include native size-ability group scale.
Use the source knee's front direction through full extension rather than the
unstable projected knee position. When lowering cannot solve horizontal reach,
bound the support lean to a feasible pose; never silently displace a boot.

The support state belongs to the current rig. Ordinary release fades it; guard,
melee, ragdoll and other exclusive owners interrupt it. Takeoff fades existing
support without acquiring new airborne support or retaining ground foot anchors. Restoration must compose
with source gait, chest, final spine and form replacement. No source-bank edits,
dependencies, map changes, saved-profile writes or git commits. Continue inline
under the user's standing autonomous implementation authority in PRODUCT.md;
preserve the shared dirty worktree. Use bounded independent review after tests.

Proof: RED first-launch witnesses at 30/60/120 Hz; final animated ray readiness,
no paid packets before preparation, fixed foot targets and orientation, actual
rendered foot/arm contact, source stride continuity, release/guard/form/KO,
ground-to-air transitions and unchanged simulation motion. Record before/final
grounded elevated shots over multiple phases and views with source/procedural
labels. Re-run the final-spine, axial, hand and ground suites, full root tests,
build and actual browser input/editor flow. Report known cloth and art gaps.
