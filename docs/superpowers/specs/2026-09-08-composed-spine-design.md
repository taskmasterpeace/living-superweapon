# Composed spine and physical axial emission

The original goal and ten-part brief remain authoritative. This pass addresses
measured torso/pelvis twisting and reversal snaps, not whole-game acceptance.
Current native probe: strafing chest fire reaches 94.47 degrees of final yaw;
several reversal samples change the relative torso by 27–31 degrees in 1/60s.
The local directional budget is followed by another chest correction.

## Chosen contract

Add one final, reversible upper-body envelope after directional/chest planning
and before head/hand articulation and contact. Evaluate the composed torso in
the pelvis frame. Preserve the existing approximately 69-degree yaw budget
(1.2 radians), applied to the final result, and bound ordinary relative rotation
at 12 radians/s. This is a gameplay articulation budget, not a clinical claim or
a complete pitch/roll anatomical model. Exclusive guard/melee/physics states keep
their existing ownership. A new rig must not inherit old part references.

The final solve may reveal that the requested axial ray is not yet reachable.
Outgoing native eye/chest energy must then follow the actual final emitter;
already-fired packets retain their velocities. Planning still pursues the aim
command through the authored hose steering. First launch still waits for its
captured target rather than silently substituting another initial target.

When active travel leaves the spine at its limit, a bounded visual heading bias
lets the body reposition. It must converge and recover without changing velocity,
position, mouse aim, attack costs, physics, or the gameplay camera. This bias is
presentation state, scoped to the current rig, not a second movement controller.

Rejected alternatives: clamping only the chest's additional rotation does not
bound the final spine; clamping only the torso would detach emitted beams;
removing lower/upper-body independence would violate the user request.

## Evidence required

Real production tests must fail before the repair and pass after. Inspect final
relative rotation, source/emitted-ray alignment, uninterrupted travel, initial
launch progress, source-gait continuity, form replacement, and release. Include
source gait metadata and procedural overlay labels. Use full native sequences,
front/profiles/rear captures and an integrated gameplay check, not a flattering
still. Re-run previous optic/chest/cofire, hand, contact and startup regressions,
then the full root suite and build. Existing cloth failures must be reported.

No map work, roster expansion, dependency migration or git commit is included.
The user's standing autonomous implementation direction in PRODUCT.md applies;
the dirty shared worktree and all unrelated edits are preserved.
