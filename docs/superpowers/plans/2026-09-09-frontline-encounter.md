# Frontline encounter slice

Spec: `.dream-loop/brief.md`, creator's approved Hero on the Front Line direction and current GO instruction.

## Global Constraints

Keep native `powerworld.html`, native third-person controls, unrestricted superhero flight, current melee impact and traveling hose beams. No city substitution, height decks, auto-scaling enemies, purple, cape work, paid assets, or external publication. Preserve the shared dirty checkout; no commits, resets, staging, or worktree creation. The implementer is not alone: parent owns `powerworld.js` and `frontline-terrain.js`; do not edit those files. Do not dispatch subagents. Match/replay cleanup must release owned objects and not leak state into duel/city modes.

### Task 1: Optional small clone recovery encounter

Ownership: new `src/engine/frontline-encounter.js`, focused integration in `src/engine/game.js` and `src/engine/pwTitle.js`, tests `tools/frontline-encounter.test.mjs` and `tools/frontline-encounter-browser.mjs`. If another file is required, ask controller first. Before editing copy these existing owned files into `artifacts/frontline-encounter-baseline/` for scoped review.

Implement an optional encounter choice on the actual PowerWorld title: existing sparring remains default; a clearly labeled 'Clone recovery' option launches with `encounter:'frontline'`. Only one human for this slice; two-player selection must remain sparring or visibly disable the encounter option. No new mode id. Preserve all existing roster choices and controls. Expose the encounter state at `game.ms.frontline`.

Create a compact encounter with four ordinary ground-bound clone rifle soldiers using the existing SARGE rig/weapon and native Fighter damage/AI/ability pathways, with a per-instance copied definition (never mutate ROSTER). Clone abilities only native rifle and appropriate guard: no flight, airstrike, superhero area attacks or unlimited energy. Assign clear clone labels, no new canon hero identity. Reuse honest AI awareness/reaction and modest finite rifle damage; no hidden perfect aim or level matching. Keep their roles simple in this slice. They share a squad team opposing the player, do not respawn. Don't set `_openSky` on these soldiers: modify PowerWorld setup and late-arrival gates to respect an explicit instance `_frontlineClone` flag. Their native `flightTier=0` must keep them grounded even when the hero is high above. Preserve knockback/ragdoll; do not pin their y or disable gravity.

Place the squad and a visible, modest sample case in clear ground near the native starting area, avoiding registered cover. The objective is a recovered sample already in the case; do not fabricate blood collection from existing hits. Destroy/defeat the squad, land within 12 units of the case to secure it (hold proximity for 1.5 seconds, living and grounded, pause progress while airborne/outside/under attack), then return to a visible extraction point near the player's start and hold grounded proximity for 2 seconds. Show unambiguous phase/progress in a compact warm/amber in-match HUD, pointer-events:none; no new combat key. Flying cannot complete either ground interaction but remains fully available for approach/escape. 'Sample secured' and 'Sample extracted — research not yet implemented' are truthful outcomes, not attribute unlocks. Native flight should not be reduced or forced off. No automatic research/clone empowerment yet.

HUD and markers are encounter-owned, disposed on mode exit/rematch. Successful extraction can leave the free-roam proving ground running with a completion display; do not force a city/title switch. Allow clean repeat via existing rematch/title path. Reuse available local native audio for success if sensible; do not claim sound-library routing for unconnected cues.

TDD: write and observe focused failing tests before implementing phase logic, airborne exclusion, dead/paused exclusion, damage interruption, no mutation of roster, finite/grounded clone definitions, one-shot completion and dispose/rematch cleanup. Then implement minimum behavior. Run relevant existing PowerWorld tests once at final verification. Browser proof on real PowerWorld entry: select encounter through UI, native start, verify four grounded clone fighters and objective HUD, native player Space ascent leaves clones grounded, no console errors, return to title/rematch and no duplicate HUD/markers/clones. A deterministic fixture can suppress incoming fire for interaction phase assertions but must be labeled; don't claim entire fight won through real player inputs. Ask controller for exclusive GPU lane before browser work; use Chromium channel 'chromium' on live port5180, never stale server or root city.

Write the full report to the SDD task report with RED/GREEN commands and outputs, changed files, native versus fixture proof, and limitations. Return status, test summary, report path, concerns only. Review is handled by controller.
