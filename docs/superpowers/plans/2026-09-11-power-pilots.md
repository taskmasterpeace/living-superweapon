# Approved power-pilot implementation

Authority: user approved the five-step plan in docs/reports/2026-09-10-powers-audit-and-repair.md, then supplied comet_cN0fMlI4BH.mp4 as movement/energy-source reference.

## Global Constraints

Work only in D:/lsw/.worktrees/sarge-authoring-integration, runtime http://127.0.0.1:5182/powerworld.html. Preserve centered BFP chase, original roster art, desert and DESIGN.md (Comic impact. Tactical clarity.). No aircraft expansion. Keep the traveling beam tip and authoritative contact. Do not delete saved custom characters or authoring alternatives. Native input evidence and staged evidence must be labeled separately. No claim of full visual acceptance without sequence review.

## Task 1: Diagnose activation rows

Reproduce RIME.shift, VOLT.shift, WARDEN.shift, TORCH.shift and VOLT.lmb with native input and neutral controls. Measure starts, displacement, cost, target acquisition, collision and damage. Distinguish benchmark state/terrain contamination from game bugs. Fix only proven causes with focused regressions, never loosen pass thresholds. Owned first by read-only diagnosis agent; runtime patch assigned after findings.

Diagnosis is complete: artifacts/power-pilot/activation/findings.md and harness-probe.json prove all five activate with native input, and earlier explosions contaminate the bench lane. Implementation ownership is src/bench/abilities.js and tools/pilot-bench-isolation.mjs only. Reset destructible terrain and scorch decals at the per-row isolation boundary; place caster and dummy at world.heightAt for their respective X/Z coordinates, including groundY and presentation/spawn placement where required. Preserve existing source/target distances, timing and >6-unit criterion. Write/run a failing browser regression that deforms the lane and proves the next stage resets it; then verify all five unchanged checks and deliberate inert-handler calibration. Use port 5182. No runtime movement/power changes. Record exact commands/output and commit only owned files.

## Task 2: Curate three pilot kits and restore web-control identity

Ownership: src/data/characters.js pilot definitions only; a focused reusable web-control module and necessary narrow entity/projectile/ability integration; focused alternative-catalog data/Studio controls; new tests prefixed pilot-. Do not modify flight-pose, flight-tuning, beam-source presentation or other characters.

WEBLINE: retain Web Snare, Web Zip, Spider Flurry, Sting Kick and Danger Sense. Replace Web Darts' explosive volley semantics with a real traveling, non-homing web-control shot: short non-stacking movement slow, visible web wrapping at the contacted target, stronger targets recover sooner, frontal guard/phase/immunity reject it, no through-cover status. Do not seize the target as a grab, disable attacks, accumulate permanent slow, or make unlimited stunlocks. A small direct hit is permissible; no area explosion or collateral webbing. Base duration 1.2s, move multiplier .45, duration scales down with target strength, post-effect immunity 1s. Duration must be finite and source interruption/KO/kit cleanup must not orphan resources. Respect same simulation clock and source-owned cleanup. Use a per-power payload field rather than hero-id checks in physics. Keep generic energy volleys unchanged.

Remove Maximum Spider from WEBLINE's default equipped kit, Perfect Wave from APEX's default kit, Invincible from VANGUARD's default kit. Preserve the exact previous definitions as named authoring alternatives, accessible in Studio through a deliberate selector and persisted through the existing save/load/package path; do not overwrite existing saved custom source snapshots. Keep APEX's Wave Cannon and VANGUARD's Unbreakable as their single beam and defensive buff respectively. Empty default slots must be safe in HUD, wheel, native input, AI and validation. Do not replace cut slots with more generic powers.

Tests first: real hit/status/movement; guard, resistant/strong, blocked, phase; repeated shots don't stack; expiration/recovery; interruption, KO, form/kit cleanup, disposal; energy denial; alternatives remain selectable and survive profile round trip. Keep existing web snare/zip tests passing. Report any acceptance gaps instead of claiming all powers approved.

## Task 3: Reference-driven presentation

Inspect native backward flight before edits and compare with supplied video. Correct lost backward-leg pose through real production animation if reproduced. Prefer bent asymmetric braced legs while still facing the enemy; no backwards running in midair. Preserve authored overrides and forward flight attack continuity. For beam visual variety, inspect current source flare and external LinearAbilityCasting source, then present a bounded source/release variation proposal grounded in the video. Do not make all beams giant or replace source-to-contact logic with an ornamental laser. Keep effect source provenance honest.

Native baseline now exists in artifacts/power-pilot/flight-baseline and flight-before. Inputs are real, actor/camera/pose are unmodified. The second baseline experienced a transient HMR parse error during another task; it is inspection evidence, not a clean acceptance run. Telemetry proves backward state and joint targets activate, but chase-camera frames show almost dangling legs. Fix presentation, not input routing. Give backward default hips stronger asymmetric lift (initial targets -.78/-.4 radians), knees 1.2/.8 radians; verify against real rig at 30/60/120Hz, ascending/descending, ranged casting, forward/reverse transitions and authored overrides. Adjust only if actual knee/boot/hip geometry proves these initial targets wrong and document why. No new sway or per-frame randomness. Ownership: src/data/flight-tuning.js, src/engine/flight-pose.js only if necessary, new tools/pilot-retreat*.mjs tests. Do not edit other runtime systems. Beam work in this task is a grounded proposal, not an unreviewed new shader stack.

## Task 4: Capture and review

Record native normal-spawn pilot interactions, charge/fire/release/backward flight, and interruption. Review continuous or ordered phases. Tests are not feel approval. Record current code, viewport, GPU/quality/performance when available and errors. Independent reviewer checks scoped code and visual/action evidence. Report unfinished gates.

## Task 5: Close held-power interruption gaps discovered in acceptance

Review amendment: include sleepT and downedT directly in the same admission policy, with immediate first-controller-tick tests before Fighter.update can supply a stagger proxy. Preserve Second Wind/recovery inputs and exclude hitstop. The controller-order gap was confirmed during independent review.

Ownership: src/engine/game.js and src/engine/abilities.js narrow interruption paths; new tools/pilot-held-lifecycle.test.mjs. No changes to flight, default kit selection, web-control or shader files. Controller reproduction with mainCombatFixture, APEX Q started through runSlot and next tick through real controlPlayer: stunT=1 + KeyQ still held deals 2.2 HP; frozenT=1 deals no damage but retains `_loop` with zero stop calls; staggerT=1 correctly stops loop and damage. Fix confirmed behavior without altering damage balance or stuns themselves.

Use one consistent incapacity admission policy for retained cone/lifedrain actions through actual human and bot controllers: stun/freeze/grab/KO must stop ongoing sustain and prevent more damage/heal/shove; dead actors cannot restart loops. Preserve brief hitstop semantics (do not globally cancel paid charge merely because time is frozen). Clearing effect owners must be idempotent, and physical key-up/repress after recovery must work. Existing charged beams/remote shots should retain their tested contracts. Tests first using real Fighter, controllers and effects with an audio event observer: both APEX Consume and VANGUARD Thunderclap at interruption, exact last affordable tick then depletion (one drained feedback, no negative energy, no further receiver effect, loop stopped), KO, form replacement and repeated cleanup. For form changes, preserve existing held-attack policy unless an old owner is orphaned; explicitly test/document whichever continuity policy actually applies. Report any unrelated gaps without expanding systems. Read DESIGN.md and use the existing sound harness, not a new audio manager.
