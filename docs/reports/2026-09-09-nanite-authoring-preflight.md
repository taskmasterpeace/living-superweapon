# Nanite Task 4 authoring preflight — 2026-09-09

Read-only implementation preflight against the current shared checkout. Authority: accepted `docs/superpowers/plans/2026-09-08-nanite-forearms-brief.md:269–293`, its source/Guard contracts at :32–37, `PRODUCT.md`, and root `AGENTS.md` (no nested AGENTS found). Only this report is written. No tests, browser, source/assets, storage, installs or commits were run/changed. Task 3 contact implementation is concurrent: this report consumes its accepted winning-contact/result contract, not an approval of its work-in-progress code. Source line numbers can move.

## 1. Smallest coherent slice and priority gaps

- Keep the existing Studio, native Fighter, native slots, native MeleeSystem and Projectiles. Add one explicitly labeled `nanite` contact pattern, with shield-shot, cannon-shot and incoming-punch sample selection as transient encounter state. Do not add a second damage implementation, new engine, saved input timeline or fake physics multiplier.
- `StudioCombat.supportsAttackRehearsal` currently recognizes only beam/projectile/volley/charge/rifle/construct (`src/tool/studio-combat.js:16–18,96`). A real cannon qualifies as `charge`; the shield's `naniteShield` type does not. The genuine shield must enter the source-qualified attack/slot lists once Task 3 publishes it. Do not enable arbitrary `{type:'naniteShield'}`/renamed normal sources merely to make a select option work. Both family-alone cases and the public opposite-arm pair are required.
- Generic `StudioCombat.step` manually advances slot cooldowns/action poses and animates Fighters; it does **not** call their full update (`studio-combat.js:233–315,332`). Adding only `advanceNanites` makes an attractive assembly but an incomplete defensive rehearsal: native hitstop, guard pose/parry age, recovery, barrier cost and Guard regeneration remain wrong or frozen after the first real hit.
- `StudioPreview.duration` needs a nanite-pattern `8` branch before generic charge/construct duration logic (`studio-preview.js:68–77`). The current default charged rehearsal is **8.4 seconds**, not eight (`.6 + 1.8 + 6`). Do not shorten all charged or timed-construct previews.
- A nanite contact pattern is neither the existing interception pattern nor an AI match. Both `StudioCombat`'s final phase override (:317–319) and `studio-main.js:64–65,304–310` currently treat every non-`target` pattern as opposing-fire/interception. Branch the new pattern before those generic fallbacks, otherwise its truthful damage/reform readout is overwritten.

## 2. Native clocks, Guard and contact order

### Clock ownership

The existing resource split is correct to reuse, not to erase: `StudioPreview.step:224–230` passes simulation `dt=0` during settling but positive `poseDt`; the resource branch regenerates once after both primary/secondary inputs, settles upkeep, updates projectiles, then constructs (`studio-combat.js:299–315`). Ordinary/timed previews retain their legacy order. `tools/resource-construct-studio.test.mjs` contains real Preview-prototype fixtures for this exact distinction and both primary/secondary resource orders.

Native Fighter evidence explains the extra nanite requirement:

- `Fighter.update` checks source identity/retirement first, then native status clocks (`entity.js`, `update`, approximately :1174–1198). `regenerateKi` is the shared bounded ordinary calculation (:1153–1155), including charging/sustain, sheet, mood, wounds, max clamp and infinite core.
- Native barrier drain, cooldowns, `_blocked`/stagger recovery, meter recovery and `melee.update` precede pose/regen (:1444–1456). `poseGuard` is damped before final animation (:1495–1497); the shield pose adapter requires `poseGuard>0` (`nanite-pose.js:61–63`). Setting `guarding=true` without these clocks is insufficient.
- Ordinary regeneration and native Guard recovery occur before the hitstop return (:1505–1549). In `_openSky` Studio, Guard stays a block, not city's defenseless `chargingKi`, but still grants the native 22/s extra recovery while unblocked; barrier is its separate native rule. Do not copy a simplified “8/s plus a shield” formula or force `chargingKi` to manufacture protection.
- Nanite assembly/repair advances only **after** the hitstop return, before physics/final action pose (:1547–1557). A frame which starts with positive hitstop, even less than `dt`, freezes nanites for that entire frame. KO/frozen early-return paths also do not advance assembly/repair. Decrementing hitstop first and then testing it incorrectly repairs on the last held frame.
- `advanceNanites(state,dt,unlockedSlots)` (`nanite-state.js:20–35`) ticks all equipped modules; unlocks, deployment and each damaged cell determine their own progress. Neither selected-slot-only ticking nor mesh-count-derived readiness is correct. `_animate`/`presentNanites` only present this state.

**Recommended smallest no-engine-edit contact path:** a nanite-only native step branch in `StudioCombat`, using the same `Fighter.update`/`MeleeSystem` production boundary already exercised by `studio-melee.js:10–28,31–72`. It owns just rehearsal inputs/placement and the game facade, and does not run the old manual cooldown/action/regen/nanite ticks as well. Instantiate/reuse the MeleeSystem and its native `onBlockedStrike`/`coneFoe` facade support just as `resetMelee` does, without treating its current choreography as the nanite sequence. Leave legacy actors/patterns untouched. Pure hover/flight/model inspection still needs one explicit positive-simulation nanite advance (no incoming combat); the existing `groundJump` and `melee` paths already use Fighter.update and must not receive a second nanite tick. Entering a generic cannon attack rehearsal must not leave its installed module permanently at assembly zero merely because the contact-test selector is not `nanite`.

**Motion ownership is a required implementation decision, not a hidden shortcut.** Existing `place(time)` writes scripted root positions/velocities (`studio-combat.js:164–213`), whereas a full Fighter.update also integrates physics. Blindly stacking both double-integrates the intended path; restoring roots after contact makes the visible receiver disagree with the tested one. For the new native contact branch, use the existing motion choices as transient intended movement/aim choreography and let native updates produce the final body/contact pose. Snapshot the coherent prior pose before applying any fixture-driven root displacement; do not overwrite final roots after resolution. Preserve hitstop's held root rather than snapping to a future analytic path while the body is stopped. If exact old analytic path ownership is required instead, parent must authorize a narrowly shared Fighter clock extraction first; copying guard/status/regen formulas into Studio or replacing `_physics` with a no-op is not equivalent native evidence. No engine helper extraction is authorized by this report.

### One positive step through a hit

Required dependency order for the new contact branch:

1. Compute `positiveStep = Number.isFinite(simDt) && simDt > 0 && !settling` once. Set facade time/dt from that simulation clock; only this condition may advance edges, cooldown/status/nanite clocks, native melee, projectiles, particles or VFX. `poseDt>0` is never substitute permission.
2. Build native movement/aim/Guard and slot intents. `MeleeSystem.guard` owns legality and retains held Guard through hitstop (`melee.js:337–346`). Apply it before computing busy. Use Game's busy expression (`game.js:3570,3629`) and `feedSlot:67–70`: mask pressed/held when busy, forward released; native `shift` exemption stays intact. Do not skip `runSlot` entirely merely because Guard is held.
3. Capture coherent previous body/cell transforms via native `beginContactFrame`; finalize both actors through their one native update. Both actors' final matrices must exist before `endContactFrame`. The generic stage currently animates the opposing target **after** projectiles (:332); that ordering cannot be copied into a moving shield/fist receiver fixture.
4. Resolve native melee once after both final poses, then any resource upkeep once if that mixed rehearsal is deliberately supported, then native Projectiles, then construct updates, then particles/VFX. This matches `Game.update:3903–3939` and preserves resource exhaustion before new construct fire. A melee break on this frame must disable/retire the unresolved cannon before projectile launch commitment; a committed native launch is not retroactively refunded/deleted. Task 3 owns those interruption/contact rules.
5. Present newly broken/reformed nanite state with zero elapsed simulation after contacts if necessary, then take diagnostics. Do not repair again after the incoming hit, double-step animation springs, or capture a one-frame-old intact hull. Keep `positiveStep` authority until all callbacks/cleanup finish, so `step(0)` cannot spawn a due incoming shot, rerun a toggle, consume an input edge, or call a hit-producing manager.

Native Guard transition release remains a separate regression: a paid charge may produce its one native release/fizzle when Guard masks held=false, even if released=false. Continued Guard does not permit a new opposite-arm cannon charge or a shield-toggle press. Lower Guard before a fresh press; do not change bot scheduling to mimic player/pad. `melee.update` advances `_guardUpT` (:514): without it every incoming punch may be incorrectly classified as a fresh parry. Preserve full-soak meter/blocked-strike/hitstop consequences from the accepted contact preflight, not just panel HP subtraction.

## 3. Transport, reconstruction and sound

`StudioPreview` has play/pause but **no rate** (`studio-preview.js:19,53–65,79–80`; `studio-main.js:28,349–350`). Add a transient `playbackRate` with exact supported choices 1 and .25, default 1, validated by a Preview setter. Multiply capped wall elapsed time before it enters the existing fixed **1/60 simulation accumulator**; do not pass `.25/60` into only the nanite reducer or change attack speed, phase samples, charge durations, source physics or audio pitches. A 0.25× eight-second sequence requires 32 seconds of foreground wall time. Pause remains `playing=false`, with no accrued wall-time catch-up; clear/consistently rebase fractional accumulator on a rate change. This belongs in the existing transport, not `profile.motion`, attack values, package data or local save. It can control other previews consistently without changing their 1× default.

`_seek:133–173` recreates the combat actors, applies level/form, resets the combat facade, then runs **90 positive-pose-dt calls at timeline zero** and replays fixed simulation frames. Existing `step:227` snaps combat time to 60Hz; compare snapshots at those canonical frame times (the range control's .01 seconds is not a finer simulation clock). Do not silently promise exact arbitrary-millisecond states. Ensure:

- `seek(0)` leaves `assemblyT=0`, no payment/CD/repair/contact/input events, even with 90 settling calls. Presentation can converge, but unlocked/deployed state must come from native simulation rules rather than fake readiness. Camera/view/isolation/resize and repeated `step(0)` do not run simulation.
- Forward replay and seek compare semantic state, actual cell HP/quiet/reform, payments, accepted impacts and native shot states at assembly-end, first arrival, break, reform-end and repaired shot. Epoch values deliberately change when actors are rebuilt (`nanite-state.js:4,13`); do not demand numeric epoch equality across new Fighters. Validate that old epoch/view contacts are rejected instead. Fragment seeds use epoch (`nanite-forearms.js:141`), so semantic reconstruction is not proof of pixel-identical individual fragment positions; flag exact visual-seed reproducibility if required, rather than rewriting epochs in the stage.
- At exactly 8s, stop/clear stage-owned ordnance, held-slot FX and stale melee inputs once, retaining measured totals and the visible assembled/damaged modules for paused inspection. Eight seconds is not a native nanite lifetime or forced state retirement. Repeated endpoint sampling is idempotent. Reset/swap/dispose must retire the actual old Fighter and all its geometry/loops through native disposal. The existing autoplay loop resets when the next time reaches duration (:60); explicit `seek(8)` must exercise endpoint cleanup independently, as resource tests already do.
- `StudioAudio.active` is opt-in + playing + not scrubbing + not disposed (`studio-audio.js:12–22`). Seek wraps replay in `setScrubbing(true)` with `finally` restore (`studio-preview.js:128–132`). Keep all native cues behind that facade. Existing loop handles can be recreated silently during replay and start on their next live set/ramp; pause retires physical handles/tails without replaying historical one-shots. A broken/retired cannon must stop its facade permanently, not let repair revive an unresolved voice/shot. No new sound implementation is required.

## 4. Actual incoming samples and truthful measurements

Use accepted Task 4 times, not generic .6/chargeHold choreography: initial assembly; cannon press .8/release 1.5; Guard 2.0–3.9; real incoming shot emitted 2.4; second fresh charge 4.2/release 5.0; unguarded shield toggles 6.0/6.3. Suppress generic primary/secondary/remote/opposing-fire scripts while the nanite pattern owns these edges. A source with assemblyTime=2 or repair delay=3 may legitimately deny scheduled shots or still be reforming at 8; do not overwrite authored values or auto-delay/retry inputs to manufacture a success. Record those outcomes.

Pattern eligibility should validate the effective selected source(s), not names or a catalog string alone. Support cannon-only, shield-only, and selected cannon+shield on opposite arms, in either primary/secondary ordering and either anatomical assignment. An installed unselected shield remains a native passive physical module during Guard; do not silently fire an unselected cannon. Explicitly reject/disable a non-nanite secondary for this dedicated pattern unless its extra choreography and budget order are implemented and labeled. All equipped modules still receive their native clocks. A source/level/profile change must normalize incompatible selections and stop the old script; `setCombat` must reject before assignment, not return false after partially changing a selection.

For shield-shot vs cannon-shot, derive the destination from the **current live cell layout/world matrix at emission**, after the pose is finalized. Do not aim at `fighter.muzzle()` as existing opposingFire does (`studio-combat.js:135–140`), old t=0 transforms, a generic cylinder, or a compacted hull instance index. Emit one real native projectile from the opposing Fighter; no `takeDamage`/`damageNanite` calls in the browser choreography. Use a finite-radius ordinary no-splash ballistic fixture if the objective is a single localized debit; its explicit parameters are inspection settings, not user-specified/new source balance. Defenses still scale the amount; a raw number large enough in one hero may not break a stronger hero's cell. Track emission and **actual contact** time separately. A moving target can miss; that is evidence, not permission to home/teleport the fixture into the cell or falsely announce the scheduled time as a hit.

The incoming-punch sample must invoke native `chargeStart`/`chargeRelease` on a real opposing Fighter and resolve the final swept fist through begin/endContactFrame. Reuse the stable native facade setup demonstrated by `studio-melee.js`, but do not fake a punch with a projectile or force a cell winner. Ordinary attack distance is validated 12–60u (`studio-preview.js:116`), whereas the existing real melee sample starts 4.8u apart (`studio-melee.js:17`); give this labeled close-contact sample its own transient native-reach staging and disable irrelevant distant-target controls, without relaxing the global attack-distance contract. Preserve strong/heavy crush semantics and report a whiff/body-first contact if geometry actually wins. Capture full encounter context; isolated view may hide the attacker but cannot count as contact visual acceptance by itself.

**One authoritative measurement target per sample; explicit HP policy proposal.** Current `onHit` filters to the opponent except defensive melee, increments `damage/contacts`, then immediately refills HP (`studio-combat.js:62–69`); the opponent has a billion-HP measurement pool (:105–108). That opponent is a measurement dummy, not persistent injury/death. Recommend the nanite owner retain its authored finite HP and use real native `takeDamage`/KO, with **no owner autoheal**, while the outgoing opponent keeps the existing labeled autohealed dummy treatment. Route the singular main damage meter to the equipped owner's received body damage; show its actual current/max HP and stop its attack/Guard/incoming choreography on KO while native KO presentation may finish. Reset/seek rebuilds the sample. Do not give the owner invulnerability, inflate its maxHp, change damage amounts to guarantee survival, or revive it inside onHit. Callback-only healing is not a reliable nonlethal contract: native lethal resolution may run before onHit. This is a transient inspection encounter, not a persistent-injury/save/death simulation or an AI balance result; actual gameplay lifecycle acceptance remains required. This precise stage HP policy is a proposal for parent acceptance, not a change to native balance.

Maintain separate **outgoing dummy body damage** only as labeled secondary shot evidence; never sum outgoing and incoming damage into one “actual damage” total. The required per-module `bodyDamage` should mean owner damage from winning contacts on that module, not the complete owner aggregate repeated for each slot. Owner body-first/splash hits have no local-cell attribution. `absorbed` is the actual native local absorption result; `bodyDamage` is the accepted Fighter callback amount after defenses/guard/pools. Neither is raw projectile damage nor nominal `dmgMin/max`. Full absorption records a real accepted module contact with zero body damage. Direct/splash callbacks may both legitimately damage a body; only the direct token can contribute local-cell absorption, and it contributes once. Native reflection/rejected contact contributes no module debit.

Read-only snapshot contract (produce fresh plain objects; do not leak mutable Map/mesh/cell objects into the UI):

- Required per-module `{slot,phase,intactCells,totalCells,absorbed,bodyDamage,muzzleError,liveFragments}`. Phase precedence must put KO/retired/locked/retracted and actual structural denial above generic charging/recovered; distinguish assembling, ready, damaged/reforming and obstruction. Map `_naniteDenied`/retry to structural labels, not `onNoKi` (Task 2 supplies `assembling`, `reforming`, `occupied`, `obstructed`).
- Count intact cells from state, not `hull.count` during assembly; use `view.fragments.count` for actually presented fragments. The shield may remain partially usable with a hole. Include deployment/guard eligibility and actual cell HP/quiet/reform in test snapshots rather than inventing all-or-nothing shield readiness.
- Include aggregate incoming body damage/accepted native arrivals, separately outgoing dummy damage, and bounded event records `{time,attacker,target,slot?,cell?,kind,absorbed,bodyDamage}`. Sample after contact handling, not before state updates. Use Task 3's accepted result metadata/observer; do not run its reducer a second time to calculate telemetry.
- `muzzleError` is null when no committed cannon launch exists. Compare committed physical sphere center to recorded aperture plus launch direction × full physical radius; optionally measure aperture-to-final-socket separately. Do not report socket-to-sphere-center distance as aim error or move/shrink the real sphere to get zero. This preserves the parent-accepted finite-sphere Task 2 contract.

## 5. Formation, identity and atomic import/export

Shared metadata already provides the configuration (`nanite-tuning.js:5–35`; `attack-tuning.js:242–246,388–404`). Add a compact **Formation** group inside the current Attack inspector, not a new schema/editor: real source family label, anatomical forearm, contour/dimensions/offset, assembly/material/density, cell integrity/quiet/reform/breakaway, and cannon-only charge stages. Existing `attackEnum`/`attackNumber` and delegated `data-attack-key` commits are reusable (`studio-main.js:89–120,177–193`). Spell out that integrity/repair are gameplay changes and initial source values are unbalanced engineering defaults, not user numbers; do not bury them under the Effects tab's “appearance, never damage or protection” claim (:125). Display “Forearm cannon,” not Palm projection. The family is chosen in ORIGIN/Edit power kit, never by editable `naniteForm/type`.

`materializeAttack` derives internal `castHand` from `naniteAttachment` (:51–55); native runtime arm-name reversal must not leak into anatomical UI labels. Source-qualified fields deliberately exclude emissionOrigin/faceOrigin/chest/castStyle/castHand. Shield fields exclude cannon charge stages and generic projectile controls. Same-arm loadouts reject atomically (`validateNaniteLoadout:37–43`; `applyAttackOverrides:449`). With both modules equipped, two sequential single-arm edits cannot swap them: each intermediate collides. Offer either a single atomic “Swap forearms” transaction that validates both sparse entries together, or clearly require a temporary kit change; do not disable whole-loadout validation to make the swap appear convenient. An atomic pair swap is the small usable proposal for this UI.

Preserve sparse unedited sources. `setAttackOverride:407–430` stores only values different from genuine source defaults, validates the effective loadout, and does not bake all normalized nanite defaults into every source. `attackIdentity:236–239` hashes stable **whole source data**, not just type/name. `reconcileAttackOverrides:379–385` drops mismatches; `applyAttackOverrides:437–453` applies only genuine local sources, never imported snapshots. Source replacement must not retain a stale cell configuration even if the replacement is another charge. Profile values cannot add a nanite family to an ordinary source.

Actual package boundary: `validateCharacter` allowlists catalog power IDs and recipe fields (`character-package.js:20–44`); `importCharacter:50–57` rebuilds via `buildDef` from local POWERS, applies profile to that genuine def, then makes one storage write/new ID. `buildDef` clones the catalog ability (`creator.js:298–302`). It does not trust a profile's source snapshot as a new catalog attack. Current snapshot publishes cannon at `creator.js:108`; shield publication belongs to Task 3's completed contact gate, not this preflight. Task 4 must test the resulting public pair, not create an extra private menu substitute.

**Concrete atomic-validation gap to cover before mutation:** `validateProfile:75` validates attacks without a def, so it cannot detect an effective two-arm conflict against an unpatched source. `saveProfile:150–152` likewise has no source context. Current profile import (`studio-main.js:367–369`) reconciles identities, then selects/pushes history before `refresh` applies the profile. `StudioPreview.setProfile:85–94` assigns its def/profile and disposes the old Fighter before constructing `new Fighter(applyProfile(...))`. A syntactically valid same-arm patch can therefore fail too late and strand an invalid draft or disposed preview. Add an effective `applyProfile(genuineDef,candidate)` / `validateAttackOverrides(attacks,genuineDef)` preflight **before** history/save/live replacement. Do it inside the authorized Studio boundaries; no generic profile-format migration is needed. Preserve existing draft, actor identity, storage, selection and playing/sound state on rejection.

Package import is safer: effective applyProfile occurs before saveCustom/roster mutation. But `validateCharacter`/`exportCharacter:46–48` alone only perform schema validation; export should also preflight the draft against its genuine local def in Studio. Tampered source snapshots with recomputed identity are valid-shaped but must reconcile away, never become runtime attacks. Unknown preset/form/type/field, unsafe nested key, nonfinite/out-of-range values, charge-stage gap and duplicate effective forearms must reject. UI-local validation does not expand low-level saveProfile's API promise; if low-level callers themselves must reject source conflicts, request separate narrowly justified production authorization.

## 6. Minimum discriminating fixtures for the implementer

Use `tools/nanite-studio.test.mjs`; borrow the real Preview-prototype/no-WebGL shell pattern from `tools/resource-construct-studio.test.mjs`. This preflight ran none of these:

1. Public ORIGIN cannon-only, shield-only and opposite-arm pair, both slot orders/arm assignments. Tuning field availability, selecting shield, source-specific pattern eligibility, unlock/form changes and no nanite allocation for ordinary sources. First assembly press before readiness is denied without payment; long authored assembly/reform remains truthful at fixed times.
2. Actual `_seek(0)` including all 90 pose-settle calls, repeated zero step, all views/resize/isolation, pause, 1×/.25× equal **simulation-time** snapshots. Compare frame-canonical state without requiring rebuilt epoch numbers equal. Partial positive-hitstop frame freezes repair; next free frame resumes once. Guard timers and guard meter/recovery continue exactly as native rules prescribe, not a manual timer approximation.
3. Native incoming projectile at current moving cell, genuine shield full soak/partial residual/body-first miss, cannon debit with owner damage, and native incoming fist using both entity iteration orders. Confirm exactly one local debit; main meter is owner-only; outgoing dummy results are separate. Capture actual arrival after scheduled emission. No direct damage calls in the rehearsal harness.
4. Guard-before-busy masking on .8/1.5/2/3.9/4.2/5/6/6.3 edges; additionally an already paid charge entering Guard, a denied held press followed by repair without fresh press, and frozen/stagger/KO interruption. Post-repair success needs a new legal press. Active-state timers, native pending launches and audio handles must not resurrect old work.
5. Exact endpoint8 with a slow/missed shell remaining at7.9, no ordnance/loop/input edge afterward, retained readable cell/totals; reset/swap/dispose resource counts return to baseline; pure model, melee and native jump clocks do not double-advance installed modules. Existing timed/resource/ordinary attack timing and no-regen legacy fixtures remain unchanged at1×.
6. Save/Undo/Reset/export/import of the real catalog pair, atomic swap transaction, source-replacement reconciliation, forbidden origin/family injection, malformed/duplicate-arm import with no draft/preview/storage mutation, and import new-ID behavior. Rate, pattern, sample, timeline, HP meter, cell state and preview starting values never enter saved profile/package. Reference existing `attack-tuning.test.mjs:50–76` plus profile/package fixtures; their private schema tests alone do not prove public UI round-trip.
7. Existing StudioAudio fake-backend fixtures prove no historical sounds during seeks and live charge resumes only on a later positive step. The later required browser sequence must verify moving assembly/impact/hole/reform/firing, actual native input outside Studio, both body families/arms and geometry fit; this report makes no visual/feel/audio acceptance claim.

Implementation ownership stays the accepted three Studio JS files and new Task 4 tests/harness/report. Read-only consumption of tuning/profile/package and native contact APIs is sufficient unless a discriminating RED proves a missing shared seam and parent grants that precise file. No broad migration, catalog publication ahead of Task 3, movement-bridge edit, audio rewrite or balance adjustment is authorized here.

Skill application: `warworld-animation-authoring` plus its acceptance matrix require moving armed/contact evidence, final production pose order and no approval from a still. Its TypeScript-era file paths remain absent here; native JS Fighter/Studio/test seams are the explicit adaptation. `three-webgl-game` and its architecture reference support separate simulation, presentation, DOM controls and diagnostics; their general engine/asset preferences do not override this checkout's procedural Three.js or permit a physics migration.
