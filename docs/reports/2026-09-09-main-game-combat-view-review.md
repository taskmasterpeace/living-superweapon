# Main-city combat view — independent Task1 review

## Spec compliance

**❌ Issues found.** The camera/input policy and native-city separation are implemented, but the required combined combat/city HUD footprint is incomplete: the sundial paints through the foe's name and HP during the supplied native beam encounter. The supplied two-player screenshot is also not acceptable framing evidence; its inversion/clipping remains under investigation, not attributed to a guessed camera fix.

**⚠️ Acceptance not established by this review:** real-root KO/respawn and map-tool walkthrough, wanted-positive/extreme warning density, steady wide two-player fit, and complete matched body/cannon/guard/near-cover acceptance. Subsequent body captures are partial (see final evidence append), not a complete firing/footing pass. Passing camera flags are not a substitute for these frame checks. Physical gamepad/phone feel and whole-game/AAA quality are not claimed.

## Strengths

- `src/engine/combat-view.js:4–14` is a small, pure ownership policy. It separates `hasCity`/view ownership from `_openSky` and physics; configured two-human sessions remain shared even with a dead second human. `game.js:3303`, `4073` and `hud.js:1971` consume the same policy instead of introducing a second simulation mode.
- `world.js:2588–2595` exposes an explicit BFP style while retaining the three-argument legacy dispatch. The task-attributed change does not replace the existing 25.5 rear / 9 lift / 73.74° / root+5.4 solver or its collision/profile seams.
- `game.js:3317–3326` establishes the actual perspective eye before controller aim; `3363` traces the native physical ray. Hostile selection is separated from physical ally contact. City movement and evade use planar camera axes without assigning PowerWorld flight flags (`3529`, `3554`).
- `game.js:3286–3313` retires held attacks/input at ownership/liveness boundaries without advancing simulation. `core/gamepad.js:63–82` keeps R3 suppression through the release edge; tests cover either **release** order, real standard-pad polling/deadzone, standalone item use and ordinary L3 descent. The explicit native touch lock keeps the existing control surface (`core/touch.js:59`, `79`, `134`).
- New tests exercise native World/Fighter/controller/ordnance methods rather than an invented aim implementation. The report accurately distinguishes the helper's omitted WebGL construction from real-root evidence, passive staged rivals from unassisted combat, touch emulation from hardware, and traveled beam contact from a launch-only assertion (`tools/main-combat-view.test.mjs:32`, `43`, `149`; `tools/helpers/main-combat-fixture.mjs:11`; `tools/main-combat-view-browser.mjs:84–133`).
- The warm/gold compact status and selected-attack docks leave the central body lane substantially clearer in the inspected desktop beam frame while retaining the actual city label and correspondent. The semantic `powerworld` CSS remains separate from `combat-chase` (`hud.styles.js:966–1063`).

## Issues

### Important — should fix

**P2 — Reserve a non-overlapping city-clock/foe-status lane in the compact chase layout.**

- Anchors: `src/engine/hud.styles.js:1015–1020` (new city footprint adaptation), `88–90` (existing centered foe panel), `563–575` (existing centered sundial); `tools/main-combat-view-browser.mjs:153–164` (layout gate only measures city/status/attack/news/wanted boxes).
- In `artifacts/main-combat-view-combat/native-beam-hit.png`, the sundial needle and bright sun cross the target name and its HP bar. Both elements are centered at the top: the foe begins at y=16 with a width up to520px; the246px sundial begins at y=0 and its SVG allows overflow. The new adaptation relocates the city plate but leaves this other city/combat intersection intact.
- This is a missing part of the requested combined-footprint adaptation, **not a claim that this task invented the two old absolute anchors**. Target identity and HP must remain legible while the living-city clock remains truthful. Give these surfaces separate reserved space under the chase class, or compose them in a bounded flow; do not solve it by spoofing PowerWorld or globally removing the city clock/news. Add a focused DOM/frame regression with an actual visible foe and the moving clock needle, not only empty/zero-wanted layout.

### Held evidence issue — do not invent a root cause

**The supplied post-opening Duel2P frame is inverted/clipped despite correct policy flags.**

- Anchors: `src/engine/game.js:4088–4104`, `src/engine/world.js:1386–1410`; current tests `tools/main-combat-view.test.mjs:177–187` and browser assertions `tools/main-combat-view-browser.mjs:130–132` check mode/capture/classes, not projected upright bodies or active render-camera identity.
- I inspected `artifacts/main-combat-view-combat/root-duel-2p.png`; the subjects and ground label are visibly inverted and a body is clipped at the top. The report's “iso/Orthographic/no capture” statements are true but do not establish successful shared framing.
- The parent's proposed inherited-BFP-eye cause is **not established**. A focused native no-WebGL control at prior BFP pitch0/+0.9/−0.9, yaw2, followed by real `prepareCombatView`/`cameraDrive` into two humans, produced positive projected root-to-head vertical separation at frames0/1/4/29/60 (approximately+0.1022→+0.0998 NDC). `World.follow` recomputes its eye from positive `camDir` immediately; it does not damp the old BFP eye across the subject. Its inherited focus does settle over time, but that alone did not invert this control.
- The actual root transition must discriminate camera projection/up/quaternion/frustum, `camDir`/`camDist`, RenderPass camera identity, and final rendered actor transforms at the bad frame and steady state. Do not silently widen the fixture or fix an assumed camera formula. The GPU/root timing lane remains parent/worker-owned.
- **Subsequent actual-root control inspected:** `artifacts/main-combat-view-transition/results.json` and `settled-2p.png` use ordinary mode UI after free BFP mouse look, but do **not** replay the whole earlier combat sequence. All six post-skip samples across0–2400ms are upright: screen-normalized head-minus-root Y is−.0144→−.0363 for P1 and−.0134→−.0344 for P2 (negative screen Y is upward). Camera up remains[0,1,0], frustum top positive/bottom negative, and native RenderPass camera equals `world.camera` throughout; `errors=[]`. The inspected settled image is upright and both subjects are in frame at the original40u separation. This rejects a universal BFP→shared inversion claim, but does not explain or erase the original failed screenshot, nor prove wide fit. No runtime camera correction is justified from the disproved simple hypothesis.

### Minor / bounded legacy limitations

- **P3 — Phone mood overlaps the native meter strip.** `hud.js:821–834` retains the body's fixed `bottom:250px` mood anchor outside desktop docking; the new selector intentionally excludes `.phone/.tablet` (`hud.styles.js:1007`). At844×390, `landscape-844.png` shows the mood tile covering part of the guard/XP area. HP and energy remain visible. The report explicitly discloses it; this is not newly attributed physics/input breakage or a reason to remove the existing portrait rotation/platform rules. It remains a real mobile readability limitation, not a polished-phone pass.
- **P3 — Reported output is not warning-clean.** The build's large-chunk warning and detector's8 existing findings are disclosed in the implementation report's Verification section. They concern earlier code, not the task-attributed compact block; no unrelated cleanup is requested. Preserve that distinction in the handoff.

### Explicit follow-on boundary — beam pose/readability is not camera parity

At the parent's request I checked the native launch boundary, without expanding into a pose fix. `projectiles.js:764` derives BeamHose `_combatReadability` from `caster._openSky`; `796` independently gates `_poseLaunch` on the same flag even though `Game.spawnBeamFor` supplies `poseLaunch:true` (`game.js:3227`). Thus city BFP retains the prior city's immediate-launch and legacy beam-visual branches. The new view does not make these two beam flags disagree; it exposes the legacy branch from much closer behind the actor.

The consequence is substantive but separate: actual final-emitter alignment/quiet preparation is not established for city beams by the existing open-sky startup and axial tests (`tools/beam-startup.test.mjs:18`, `tools/axial-launch.test.mjs:20`). The physical readiness/payment path lives in `projectiles.js:1118–1154`, while sustained final axial direction is conditional at `1277`; current combat-pose support also has explicit `_openSky` gates (`combat-pose.js:141`, `364`, `397`). Do not label a native traveling city beam hit as complete BFP emitter/animation parity. Do not connect readiness to `combatLookActive` or the selected render camera: that would make authoritative launch latency/payment differ between the viewed human, bots and shared/paused views. A later explicitly approved native emitter task must separate view-only brightness/readability from shared launch legality, preserving source prices, native contact and player/bot symmetry. This camera-only task did not authorize that change.

## Review method and evidence

- Read the complete approved plan and implementer report; used the task-reviewer template and scoped game-UI/audit guidance. Read the attributed hunks from the supplied299,870-byte review package in camera/control, HUD/input and test passes. The package is honestly HEAD-to-current plus full untracked snapshots, **not** an exact Task1-only diff. HEAD is recorded as `e6a757f9abcc7473a05bebd229c716621c7d5903`; the later13-file review-baseline snapshot is useful for a correction-only diff, not retrospective attribution.
- The `followHumans` hunk ends mid-function, so its remaining current-source lines were read. Focused source checks outside the task-attributed hunks were limited to named risks: native `World.follow`/camera-mode/orbit/constructor state and camera mutation callers for the inverted shared-frame hypothesis; existing foe/sundial and phone mood positioning for the visible layout collisions. Existing unrelated boot, physics, attack/audio/nanite/news changes in the large patch were not reviewed as new camera work.
- The additional parent-requested beam-launch check read native BeamHose preparation/emission, its Game spawn call, related pose gates and the relevant startup/axial test setup. The animation skill's four older `src/client`/TypeScript contract paths are absent here; its current principle of final physical emitter/moving evidence was applied to the actual native seams. No clip or full animation approval is claimed and no broad animation suite was run.
- Independently viewed the three screenshots named above and read their saved combat/phone JSON. Those are worker-produced real-root artifacts; I did not rerun or relabel them as an independent browser capture.
- Ran one read-only, in-memory native camera transition probe using `mainCombatFixture`, real `prepareCombatView`/`cameraDrive`, and THREE projection at three prior pitches. It checks the specific inheritance hypothesis, not the compositor/full boot and not the entire2P acceptance gate. No test/source/media edits, browser/GPU work, full suites, build reruns, installs, staging or commits were performed.
- Existing132+18 tests and281-module build are the implementer/parent's reported fresh evidence. Their full gates were not repeated merely to reproduce counts.

## Assessment

**Task quality: Needs fixes.** The scoped architecture is restrained and native-path tests are meaningful. The confirmed city-clock/foe readability defect needs a narrow correction; shared-render framing and the still-pending native acceptance frames must be resolved by actual evidence rather than mode flags or a speculative camera rewrite.

## Final frozen-runtime evidence append

Read the implementer's complete “Frozen-runtime acceptance append — body fixture and 2P attribution” after it became available. Independently inspected `artifacts/main-combat-view-combat-trace/root-duel-2p-settled.png` and all six camera/projection/actor rows in `results.json`. This replays the original ground/high lock, free traveling beam/contact, secondary, pause/resume and Tab→Duel2P sequence, rather than only the simpler transition control. It is upright at every sample: top91.4703→80.0026, bottom its negative, up[0,1,0], RenderPass identity correct; both head-minus-root screen Y values remain negative. The settled image contains both native starting-separation subjects. Therefore **no reproducible camera inversion defect or corrective camera rewrite is established**. Preserve the old failed screenshot as unexplained invalid acceptance evidence, not as a defect that was fixed. Wider separation remains untested.

Inspected the small-female ground cannon image, male hover cannon image and fresh large hover guard image, plus the full fresh large `cases` data in `artifacts/main-combat-view-body-large/results.json`. These demonstrate rear-view body/target visibility in those sampled native city configurations. They do **not** establish final muzzle emission: the small/male cannon frames are explicitly charge/pre-release captures, and the fresh large cannon is actually denied (`obstructed`, retry=true, chargeT0, ki106), not firing. Do not grade the female's arms-flared charging silhouette as the final native authored firing pose. The fresh large ground samples report grounded/onFoot; the combined-run male/procedural “ground” filenames have airborne gait and cannot substitute for settled support evidence. No supporting geometry was silently removed or camera/body dimension changed to improve the captures.

The large denial's empty tested cover/body lists do not exhaust native obstruction queries; the report identifies unenumerated split/interior routes. It is not evidence of a false-positive collision or a camera-scale bug. Complete source-emission/grounded/hover acceptance remains open and belongs to the explicitly separated native delivery follow-on; this review requests no pose or collision tuning based on that incomplete diagnostic.

Parent has authorized the worker's narrow clock/foe and phone mood/meter correction. This report reviews the frozen original snapshot and does **not** approve correction code not yet re-reviewed. Root lifecycle browser work is separately queued by the parent, not retroactively counted here. The next bounded ranged recommendation is recorded separately in `docs/reports/2026-09-09-city-articulated-combat-preflight.md`.
