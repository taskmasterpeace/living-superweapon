import {chromium} from 'playwright';import {mkdir,writeFile} from 'node:fs/promises';
const out='artifacts/marketing/room-ordnance-2026-09-12';await mkdir(out,{recursive:true});const b=await chromium.launch({headless:false}),p=await b.newPage(),errors=[];p.on('pageerror',e=>errors.push(e.message));try{await p.goto('http://127.0.0.1:5184/powerworld.html?hero=sarge');await p.waitForTimeout(2500);await p.keyboard.press('Enter');await p.getByRole('button',{name:'Enter with squad',exact:true}).click();await p.waitForFunction(()=>window.PW?.game?._threatRoom?.active,{},{timeout:90000});const result=await p.evaluate(async()=>{const {sweepSplitObstacle,terrainEntry}=await import('/src/engine/projectile-contact.js'),g=window.PW.game,w=g.world,results=[];for(const [a,b]of [[{x:0,y:40,z:0},{x:0,y:40,z:400}],[{x:0,y:40,z:0},{x:400,y:40,z:0}],[{x:0,y:40,z:0},{x:0,y:400,z:0}]]){const out={};const hit=sweepSplitObstacle(w,a,b,0,out,true);results.push({hit,out:{kind:out.kind,t:out.t,pos:out.pos?.toArray?.()},box:out.target?.projectileShape});}return {results,allBoxes:w.cover.every(c=>c.projectileShape==='box'),flat:terrainEntry(w,{x:0,y:10,z:0},{x:0,y:-10,z:0}),triangles:w._ghTriangles};});console.log(JSON.stringify({result,errors}));if(!result.allBoxes||result.flat!==.5||result.results.some(r=>r.box!=='box'))throw Error('Room projectile collision mismatch');await writeFile(out+'/collision.json',JSON.stringify({result,errors},null,2));}finally{await b.close();}
