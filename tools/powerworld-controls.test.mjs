import {test} from 'node:test';
import assert from 'node:assert/strict';
import {POWERWORLD_CONTROLS as keys,contextualGrab} from '../src/core/powerworld-controls.js';
test('combat bindings are independent and do not consume power shortcuts',()=>{assert.equal(keys.mouseMelee,false);assert.equal(keys.guard,'KeyQ');assert.equal(keys.grab,'KeyE');assert.equal(keys.strike,'KeyV');assert.equal(keys.digitsSwap,false);});
function fixture(){const calls=[];const f={};const game={doInteract:()=>false,pickupGear:()=>false,grabProp:()=>false,melee:{grab(){calls.push('grab');if(f.grabbing)f._personCarry.throwArmed=true;else f.grabbing={};},liftPerson(){f._personCarry={};},releaseGrab(){calls.push('throw');},setdownPerson(){calls.push('down');return true;},release(){calls.push('release');}}};return {f,game,calls};}
test('initial acquisition release does not throw; new held gesture throws',()=>{const {f,game,calls}=fixture();contextualGrab(game,f,{pressed:true,held:true,version:0},.02);contextualGrab(game,f,{released:true,version:0},.02);assert.deepEqual(calls,['grab']);contextualGrab(game,f,{pressed:true,held:true,version:0},.3);contextualGrab(game,f,{released:true,version:0},.02);assert.deepEqual(calls,['grab','grab','throw']);});
test('short management tap sets down, cancellation disarms throw',()=>{const {f,game,calls}=fixture();f.grabbing={};contextualGrab(game,f,{pressed:true,held:true,version:0},.05);contextualGrab(game,f,{released:true,version:0},.02);assert.deepEqual(calls,['down']);f._personCarry={throwArmed:true};contextualGrab(game,f,{released:true,version:1},.02);assert.equal(f._personCarry.throwArmed,false);assert.deepEqual(calls,['down']);});
