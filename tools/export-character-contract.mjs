import fs from 'node:fs/promises';
import {GLTFLoader} from 'three/addons/loaders/GLTFLoader.js';
const bytes=await fs.readFile('public/models/modular-hero/modular-hero.glb');
const gltf=await new GLTFLoader().parseAsync(bytes.buffer.slice(bytes.byteOffset,bytes.byteOffset+bytes.byteLength),'');
const bones=[];gltf.scene.traverse(o=>{if(o.isBone)bones.push({name:o.name,parent:o.parent?.isBone?o.parent.name:null,position:o.position.toArray(),quaternion:o.quaternion.toArray()});});
await fs.mkdir('public/authoring',{recursive:true});
await fs.writeFile('public/authoring/character-contract.json',JSON.stringify({schema:'powerworld-authoring-v1',rig:'ual-deform-v1',units:'metres',nominalHeight:1.8325,axes:'GLTF Y up; parts use local bone axes',bones,limits:{parts:64,keyframes:240,durationSeconds:30},materials:['suit','accent','dark','skin'],review:['front','back','left','right','top','idle','flight','contact','release','small-large pairing']},null,2));
await fs.writeFile('src/data/character-bones.js','// Generated by tools/export-character-contract.mjs\nexport const CHARACTER_BONES='+JSON.stringify(bones.map(b=>b.name))+';\n');
