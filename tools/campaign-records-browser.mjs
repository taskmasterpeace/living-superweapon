import {chromium} from 'playwright';
import {mkdir,writeFile} from 'node:fs/promises';
const out='artifacts/marketing/campaign-records-wip';await mkdir(out,{recursive:true});
const browser=await chromium.launch({headless:true});const context=await browser.newContext({viewport:{width:1440,height:900},recordVideo:{dir:out,size:{width:1440,height:900}}});const page=await context.newPage();const errors=[];page.on('pageerror',e=>errors.push(e.message));
try{await page.goto('http://127.0.0.1:5182/powerworld.html',{waitUntil:'domcontentloaded'});await page.waitForTimeout(2500);await page.keyboard.press('Escape');await page.locator('#pwCampaign').click({timeout:15000});await page.getByRole('dialog',{name:'Campaign records'}).waitFor();await page.screenshot({path:out+'/records.png'});const downloadPromise=page.waitForEvent('download');await page.getByRole('button',{name:'Export save',exact:true}).click();const download=await downloadPromise;await download.saveAs(out+'/campaign-export.json');await writeFile(out+'/result.json',JSON.stringify({kind:'native menu navigation, headless browser; not combat or device proof',errors,download:download.suggestedFilename()},null,2));if(errors.length)throw Error(errors.join('\n'));}finally{await context.close();await browser.close();}
