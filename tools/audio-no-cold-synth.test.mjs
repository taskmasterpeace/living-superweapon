import test from 'node:test';import assert from 'node:assert/strict';import {SampleBank,MANIFEST} from '../src/core/samples.js';import {AudioBus} from '../src/core/audio.js';import {SoundLibrary} from '../src/core/sound-library.js';
test('cold approved one-shot is handled without a synth or stale replay',()=>{const b=new SampleBank({ok:true,muted:false,_pg:()=>1});b._pick=()=>null;assert.equal(b.play('wpn.m16'),true);assert.equal(b.play('not-a-cue'),false);});
test('generic native gunshot selects approved M16 recording',()=>{let name;AudioBus.prototype.gunshot.call({ok:true,muted:false,sample(n){name=n;return true;}},1);assert.equal(name,'wpn.m16');});
test('runtime SoundLibrary never falls back to oscillator for missing or cold recordings',()=>{const audio={ctx:{state:'running',currentTime:1,createGain(){throw Error('placeholder must not be created');}},sampleBuffer:()=>null};const lib=new SoundLibrary({audio,storage:null});for(const id of ['flight','reload'])assert.equal(lib.play(id),null);});
test('cold charge and beam sustain do not create legacy oscillators',()=>{const a={ok:true,muted:false,sampleLoop:()=>null};assert.equal(AudioBus.prototype.charge.call(a),null);assert.equal(AudioBus.prototype.beamVoice.call(a),null);assert.deepEqual(MANIFEST['engine.charge'].f,['ai-pass/final/charge-aura']);});
