# dependencies
node_modules/

# build output
dist/
.vite/

# packaged desktop builds (75MB exe, 110MB linux tarball) — .gitignore has NO inline comments,
# so this pattern must sit on its own line or it is read as the literal path "release/  # ..."
release/
release2/

# tooling / local artifacts
.playwright-mcp/
artifacts/flight-review/
artifacts/studio-flight/
artifacts/combat-build/
.worktrees/
.dream-loop/
.claude/settings.local.json
.claude/skills/
skills-lock.json
*.log

# os
.DS_Store
Thumbs.db

.vercel
.env
.env.*
!.env.example
encyclopedia/shots/
encyclopedia/manifest.json
src/bench/orphans.txt
docs/_recovered/
