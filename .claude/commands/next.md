---
description: What to work on next in WAR WORLD — the orphan audit, ranked by payoff per hour
---
Run the `wwa-orphan-audit` skill against this repo and give me the ranked queue.

Do NOT propose new content. The question is always what is already built and not reachable:
ability types no character carries, ORIGIN catalog powers nobody has, armory rows in nobody's
hands, data modules with no importers, exported queries with no consumers, systems with no
player-facing door.

Rank by payoff per hour and lead with the single cheapest wire. For each item say what the player
would feel differently afterwards, in plain words — not what the code would do.

$ARGUMENTS
